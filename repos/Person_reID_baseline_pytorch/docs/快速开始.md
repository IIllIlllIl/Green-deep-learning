# 快速开始指南

## 环境已就绪 ✅

所有准备工作已完成，您可以直接开始训练！

## 使用方法

### 方式1: 使用脚本（推荐）

#### 步骤1: 验证环境
```bash
bash scripts/verify_environment.sh
```

#### 步骤2: 选择训练配置

**快速验证** (20-30分钟，推荐首次使用)
```bash
bash scripts/quick_train_test.sh
```

**标准Baseline** (1-1.5小时，复现原始性能)
```bash
bash scripts/baseline_train_test.sh
```

**高性能配置** (2-3小时，获得最佳性能)
```bash
bash scripts/high_performance_train_test.sh
```

### 方式2: 手动训练

```bash
# 激活环境
conda activate reid_baseline

# 训练
python train.py --gpu_ids 0 --name my_model \
    --train_all --batchsize 32 \
    --data_dir ../Market/Market-1501-v15.09.15/pytorch

# 测试
python test.py --gpu_ids 0 --name my_model \
    --test_dir ../Market/Market-1501-v15.09.15/pytorch \
    --batchsize 32 --which_epoch last

# 评估
python evaluate_gpu.py
```

## 配置信息

### 系统环境
- GPU: NVIDIA GeForce RTX 3080 (10GB)
- CUDA: 12.1
- Python: 3.10
- PyTorch: 2.5.1+cu121

### 数据集
- Market-1501数据集已准备完成
- 位置: `/home/green/energy_dl/test/Market/Market-1501-v15.09.15/pytorch`
- 训练样本: 751类, 12,936张图片

## 训练时长参考

| 配置 | 预计时长 | 预期性能 (Rank@1 / mAP) |
|------|----------|------------------------|
| 快速验证 | 20-30分钟 | 85-87% / 65-68% |
| 标准Baseline | 1-1.5小时 | 88-89% / 71-72% |
| 高性能 | 2-3小时 | 92% / 80% |

## 文档

详细信息请参考：
- `docs/复现指南.md` - 完整的训练配置和优化建议
- `docs/复现总结.md` - 环境配置总结和下一步建议

## 下一步

建议从快速验证开始：
```bash
bash scripts/quick_train_test.sh
```

训练完成后，结果将保存在 `./model/` 目录下。
