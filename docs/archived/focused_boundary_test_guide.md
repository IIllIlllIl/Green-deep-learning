# 聚焦边界测试配置说明
# Focused Boundary Test Configuration Guide

**生成时间**: 2025-11-11
**配置文件**: `settings/boundary_test_lr_dropout_focused.json`
**测试目的**: 验证保守调整后的Learning Rate和Dropout范围

---

## 📋 测试概览

| 项目 | 内容 |
|------|------|
| **测试模型数** | 3个 (DenseNet121, MRT-OAST, ResNet20) |
| **总配置数** | 13个 |
| **测试超参数** | Learning Rate, Dropout |
| **预计总时长** | ~3.7小时 |

---

## 🎯 模型选择理由

### 1. DenseNet121 (5个配置)
**为什么必须测试**:
- ❌ 旧lr范围问题严重：
  - lr=0.005 (0.1×): mAP下降15.12%
  - lr=0.5 (10×): mAP崩溃99.75%
- ⚠️ 旧dropout上界：dropout=0.7仅下降2.78%（可接受，但为统一性调整至0.5）

**新范围**:
- Learning Rate: [0.01, 0.25] = [0.2×default, 5×default]
- Dropout: [0.0, 0.5]

**测试配置**:
1. Baseline: lr=0.05, dropout=0.5
2. lr=0.01 (0.2×, NEW min)
3. lr=0.25 (5×, NEW max)
4. dropout=0.0 (min)
5. dropout=0.5 (NEW max, equals default)

---

### 2. MRT-OAST (5个配置)
**为什么必须测试**:
- ❌ 旧lr范围问题严重：lr=0.00001 (0.1×) 下降18.35%
- ❌ 旧dropout上界问题严重：dropout=0.7下降23.38%

**新范围**:
- Learning Rate: [0.00002, 0.0005] = [0.2×default, 5×default]
- Dropout: [0.0, 0.5]

**测试配置**:
1. Baseline: lr=0.0001, dropout=0.2, wd=0.0
2. lr=0.00002 (0.2×, NEW min)
3. lr=0.0005 (5×, NEW max)
4. dropout=0.0 (min)
5. dropout=0.5 (NEW max)

---

### 3. ResNet20 (3个配置)
**为什么必须测试**:
- ⚠️ 之前所有测试因checkpoint权限问题全部失败
- 📊 完全没有ResNet20的性能数据
- ✅ 权限问题已修复，可以获取第一手数据

**新范围**:
- Learning Rate: [0.02, 0.5] = [0.2×default, 5×default]
- (ResNet20没有dropout超参数)

**测试配置**:
1. Baseline: lr=0.1, wd=0.0001
2. lr=0.02 (0.2×, NEW min)
3. lr=0.5 (5×, NEW max)

---

## 📊 详细配置列表

### DenseNet121 (Person Re-ID, 60 epochs)

| # | 配置 | epochs | lr | dropout | 说明 |
|---|------|--------|----|---------| -----|
| 1 | Baseline | 60 | 0.05 | 0.5 | 基准 |
| 2 | LR Min | 60 | **0.01** | 0.5 | 新下界(0.2×) |
| 3 | LR Max | 60 | **0.25** | 0.5 | 新上界(5×) |
| 4 | Dropout Min | 60 | 0.05 | **0.0** | 下界 |
| 5 | Dropout Max | 60 | 0.05 | **0.5** | 新上界(=default) |

**预计耗时**: ~100分钟 (20分钟/配置)

**旧范围对比**:
- 旧lr下界: 0.005 → 新lr下界: 0.01 (提升2倍)
- 旧lr上界: 0.5 → 新lr上界: 0.25 (降低50%)
- 旧dropout上界: 0.7 → 新dropout上界: 0.5 (降低29%)

---

### MRT-OAST (Object Tracking, 10 epochs)

| # | 配置 | epochs | lr | dropout | wd | 说明 |
|---|------|--------|----|---------|----| -----|
| 1 | Baseline | 10 | 0.0001 | 0.2 | 0.0 | 基准 |
| 2 | LR Min | 10 | **0.00002** | 0.2 | 0.0 | 新下界(0.2×) |
| 3 | LR Max | 10 | **0.0005** | 0.2 | 0.0 | 新上界(5×) |
| 4 | Dropout Min | 10 | 0.0001 | **0.0** | 0.0 | 下界 |
| 5 | Dropout Max | 10 | 0.0001 | **0.5** | 0.0 | 新上界 |

**预计耗时**: ~110分钟 (22分钟/配置)

**旧范围对比**:
- 旧lr下界: 0.00001 → 新lr下界: 0.00002 (提升2倍)
- 旧lr上界: 0.001 → 新lr上界: 0.0005 (降低50%)
- 旧dropout上界: 0.7 → 新dropout上界: 0.5 (降低29%)

---

### ResNet20 (CIFAR-10, 200 epochs)

| # | 配置 | epochs | lr | wd | 说明 |
|---|------|--------|----|----|------|
| 1 | Baseline | 200 | 0.1 | 0.0001 | 基准 |
| 2 | LR Min | 200 | **0.02** | 0.0001 | 新下界(0.2×) |
| 3 | LR Max | 200 | **0.5** | 0.0001 | 新上界(5×) |

**预计耗时**: ~15分钟 (5分钟/配置)

**旧范围对比**:
- 旧lr下界: 0.01 → 新lr下界: 0.02 (提升2倍)
- 旧lr上界: 1.0 → 新lr上界: 0.5 (降低50%)

**注意**: ResNet20没有dropout超参数，不需要测试dropout

---

## 🎯 预期验证目标

### Learning Rate新范围 `[0.2×, 5×]`

**预期结果**:
- ✅ 所有模型的lr下界(0.2×)性能下降 < 10%
- ✅ 所有模型的lr上界(5×)性能下降 < 10%

**对比旧范围**:
- DenseNet121 @ lr=0.005 (旧0.1×): -15.12% → 预期 @ lr=0.01 (新0.2×): <10%
- DenseNet121 @ lr=0.5 (旧10×): -99.75% → 预期 @ lr=0.25 (新5×): <10%
- MRT-OAST @ lr=0.00001 (旧0.1×): -18.35% → 预期 @ lr=0.00002 (新0.2×): <10%

### Dropout新上界 `0.5`

**预期结果**:
- ✅ MRT-OAST @ dropout=0.5: 性能下降 < 10%

**对比旧范围**:
- MRT-OAST @ dropout=0.7 (旧): -23.38% → 预期 @ dropout=0.5 (新): <10%

---

## 🚀 执行方法

### 方法1: 使用mutation.py直接执行

```bash
cd /home/green/energy_dl/nightly
python3 mutation.py -ec settings/boundary_test_lr_dropout_focused.json
```

### 方法2: 使用screen后台运行（推荐）

```bash
# 创建新screen会话
screen -S focused_test

# 进入工作目录
cd /home/green/energy_dl/nightly

# 执行测试
python3 mutation.py -ec settings/boundary_test_lr_dropout_focused.json

# 脱离screen (Ctrl+A, D)
# 重新连接: screen -r focused_test
```

### 方法3: 带日志记录

```bash
cd /home/green/energy_dl/nightly
python3 mutation.py -ec settings/boundary_test_lr_dropout_focused.json 2>&1 | tee logs/focused_test_$(date +%Y%m%d_%H%M%S).log
```

---

## 📈 时间估算

| 模型 | 配置数 | 单次时长 | 总时长 |
|------|-------|---------|--------|
| ResNet20 | 3 | ~5分钟 | ~15分钟 |
| DenseNet121 | 5 | ~20分钟 | ~100分钟 |
| MRT-OAST | 5 | ~22分钟 | ~110分钟 |
| **总计** | **13** | - | **~3.7小时** |

**建议执行时间**: 选择一个3-4小时的空闲时段

---

## 📊 结果分析

测试完成后，运行以下分析脚本：

```bash
python3 scripts/analyze_focused_boundary_results.py
```

该脚本将生成：
1. 新旧范围对比分析
2. 性能下降百分比统计
3. 是否达到<10%目标的验证
4. 最终范围推荐

---

## ✅ 成功标准

测试被认为成功当且仅当：
1. ✅ 所有13个配置都能成功训练（无技术错误）
2. ✅ 所有lr和dropout边界的性能下降 < 10%
3. ✅ DenseNet121的lr边界不再出现崩溃或严重下降（>15%）
4. ✅ MRT-OAST的dropout=0.5不再出现严重下降（>20%）
5. ✅ ResNet20获得完整的baseline和边界数据

---

## 📝 相关文件

- **配置文件**: `settings/boundary_test_lr_dropout_focused.json`
- **变异范围文档**: `docs/mutation_ranges_conservative_adjustment.md`
- **完整边界测试报告**: `docs/boundary_test_analysis_report.md`
- **模型配置**: `config/models_config.json` (已更新为新范围)

---

**生成时间**: 2025-11-11
**配置模型**: DenseNet121, MRT-OAST, ResNet20
**总配置数**: 13
**预计时长**: ~3.7小时
