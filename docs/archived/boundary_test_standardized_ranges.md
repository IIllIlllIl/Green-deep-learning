# 边界值测试 - 标准化倍数表达式

## 🎯 核心原则

所有模型的超参数变异范围都遵循**统一的倍数关系**，确保测试的系统性和一致性。

## 📏 标准化范围定义

| 超参数 | 变异范围表达式 | 分布类型 | 说明 |
|--------|---------------|----------|------|
| **Epochs** | `[default×0.5, default×2.0]` | 对数均匀 | 半倍到两倍，影响训练时长和能耗 |
| **Learning Rate** | `[default×0.1, default×10.0]` | 对数均匀 ⭐ | 十倍范围，指数敏感性 |
| **Weight Decay** | `[0.0, default×100]` | 30%零值+70%对数 | 允许无正则化到强正则化 |
| **Dropout** | `[0.0, 0.7]` | 均匀分布 | 绝对值范围（非倍数关系） |
| **Seed** | `[0, 9999]` | 均匀整数 | 绝对值范围，用于稳定性评估 |

## ✅ 各模型配置验证

### 1. examples/mnist
```
epochs:        default=10   range=[5, 20]     = [0.5×d, 2.0×d]  ✅
learning_rate: default=0.01  range=[0.001, 0.1] = [0.1×d, 10×d]  ✅
```

### 2. pytorch_resnet_cifar10/resnet20
```
epochs:        default=200    range=[100, 400]    = [0.5×d, 2.0×d]  ✅
learning_rate: default=0.1    range=[0.01, 1.0]   = [0.1×d, 10×d]   ✅
weight_decay:  default=0.0001 range=[0.00001, 0.01] = [0.1×d, 100×d] ✅
               支持零值 (30% probability)
```

### 3. Person_reID_baseline_pytorch/densenet121
```
epochs:        default=60   range=[30, 120]   = [0.5×d, 2.0×d]  ✅
learning_rate: default=0.05  range=[0.005, 0.5] = [0.1×d, 10×d]  ✅
dropout:       default=0.5   range=[0.0, 0.7]   = 绝对值范围     ✅
```

### 4. MRT-OAST/default
```
epochs:        default=10     range=[5, 20]      = [0.5×d, 2.0×d]  ✅
learning_rate: default=0.0001 range=[0.00001, 0.001] = [0.1×d, 10×d] ✅
dropout:       default=0.2    range=[0.0, 0.7]   = 绝对值范围     ✅
weight_decay:  default=0.0    range=[0.00001, 0.01] = 绝对值范围 (特殊)
               注: default为0时无法使用倍数，使用绝对值
```

## 🧪 边界值测试配置 (31个)

### 测试策略: 单因子变异 (SFAT)
- 每次测试只改变**一个**超参数到边界值
- 其他超参数保持default值
- 包含baseline配置作为性能参考

### 配置文件: `settings/boundary_test_elite_4models.json`

```
MNIST (7个):
├─ Baseline: default配置
├─ epochs=0.5×default, 其他=default
├─ epochs=2.0×default, 其他=default
├─ lr=0.1×default, 其他=default
├─ lr=10×default, 其他=default
├─ epochs=0.5×default + lr=0.1×default (min+min)
└─ epochs=2.0×default + lr=10×default (max+max)

ResNet20 (8个):
├─ Baseline
├─ epochs: 0.5×, 2.0×
├─ lr: 0.1×, 10×
├─ wd: 0.1×, 100×
└─ wd: 0.0 (零值特例)

DenseNet121 (7个):
├─ Baseline
├─ epochs: 0.5×, 2.0×
├─ lr: 0.1×, 10×
└─ dropout: 0.0, 0.7

MRT-OAST (9个):
├─ Baseline
├─ epochs: 0.5×, 2.0×
├─ lr: 0.1×, 10×
├─ dropout: 0.0, 0.7
└─ wd: 0.00001, 0.01 (绝对值)
```

## 📊 性能判断标准

| 性能变化 | 状态 | 说明 | 行动 |
|---------|------|------|------|
| < 5% | ✅ 优秀 | 范围合理 | 保持当前范围 |
| 5-10% | ⚠️ 警告 | 范围略宽 | 考虑收窄 |
| > 10% | ❌ 不可接受 | 范围过宽 | **必须调整** |

## ⚠️ MNIST快速测试结果

已完成MNIST快速验证测试，结果显示：
- **lr=0.1×default (0.001)**: 性能下降6-7% ⚠️
- 虽未超过10%阈值，但需在完整测试中重点关注

## 🔧 调整建议

如果边界值测试显示某参数范围过宽：

### 方案1: 收窄倍数范围 (推荐)
```json
// 例: 如果lr=10×default导致>10%性能下降
"learning_rate": {
  "range": [0.01, 0.5]  // 从10×改为5×
}
```

### 方案2: 调整default值本身
```json
// 如果整体范围偏移，调整default
"learning_rate": {
  "default": 0.02,  // 从0.01调整为0.02
  "range": [0.002, 0.2]  // 倍数关系不变
}
```

## 📝 配置文件

- **模型配置**: `config/models_config.json`
- **边界测试**: `settings/boundary_test_elite_4models.json`
- **快速测试**: `test/boundary_test_mnist_quick.json`

---
**创建日期**: 2025-11-10
**原则**: 统一倍数表达式，系统化范围验证
