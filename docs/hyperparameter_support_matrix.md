# 12个模型超参数支持矩阵（最终版）

**生成时间**: 2025-11-05
**状态**: ✅ Seed 100%完成 | ✅ Weight Decay 90%完成 | ⏳ Precision 30%

---

## 📊 当前支持情况总览

### 图例说明
- ✅ **已支持** - 已添加命令行参数支持，可直接使用
- ⚠️ **部分支持** - 仅部分选项可用
- ❌ **不支持** - 当前未提供参数支持
- N/A **不适用** - 框架限制（如sklearn）

---

## 📋 完整支持矩阵（2025-11-05更新）

| # | 模型 | epochs | batch_size | learning_rate | seed | weight_decay | dropout | precision |
|---|------|--------|-----------|---------------|------|--------------|---------|-----------|
| 1 | **MRT-OAST** | ✅ | ✅ | ✅ | ✅ (新) | ✅ (新) | ✅ | ❌ |
| 2 | **VulBERTa-MLP** | ✅ | ✅ | ✅ | ✅ (新) | ✅ (新) | ❌ | ✅ fp16 |
| 3 | **VulBERTa-CNN** | ✅ | ✅ | ✅ | ✅ (新) | ✅ (新) | ❌ | ✅ fp16 |
| 4 | **pytorch_resnet_cifar10** | ✅ | ✅ | ✅ | ✅ (新) | ✅ | ❌ | ✅ fp16,bf16 |
| 5 | **Person_reID-densenet121** | ✅ | ✅ | ✅ | ✅ (新) | ✅ | ✅ | ✅ fp16,bf16 |
| 6 | **Person_reID-hrnet18** | ✅ | ✅ | ✅ | ✅ (新) | ✅ | ✅ | ✅ fp16,bf16 |
| 7 | **Person_reID-pcb** | ✅ | ✅ | ✅ | ✅ (新) | ✅ | ✅ | ✅ fp16,bf16 |
| 8 | **bug-localization** | ✅ max_iter | ❌ | ❌ | ✅ (新) | N/A | N/A | N/A |
| 9 | **MNIST CNN** | ✅ | ✅ | ✅ | ✅ | ✅ (新) | ❌ | ❌ |
| 10 | **MNIST RNN** | ✅ | ✅ | ✅ | ✅ | ✅ (新) | ❌ | ❌ |
| 11 | **MNIST FF** | ✅ | ✅ | ✅ | ✅ | ✅ (新) | ❌ | ❌ |
| 12 | **Siamese Network** | ✅ | ✅ | ✅ | ✅ | ✅ (新) | ❌ | ❌ |
| **支持率** | **100%** | **92%** | **92%** | **100%** ✅ | **92%** ✅ | **25%** | **33%** |

**说明**：
- (新) = 本次修改新增的支持
- N/A = sklearn框架不适用

---

### 表2: 添加少量代码后的支持情况

| 模型 | epochs | learning_rate | seed | precision | dropout | weight_decay |
|------|--------|--------------|------|-----------|---------|--------------|
| **1. MRT-OAST** | ✅ | ✅ | ✅ | 🟢 | ✅ | 🟢 |
| **2. bug-localization (sklearn)** | ✅ | 🔴 | 🟢 | N/A | 🔴 | ✅ |
| **3. pytorch_resnet_cifar10** | ✅ | ✅ | 🟢 | 🟢 | ❌ | ✅ |
| **4. VulBERTa-MLP** | ✅ | ✅ | ✅ | ✅ | 🔴 | 🟢 |
| **5. VulBERTa-CNN** | ✅ | ✅ | ✅ | ✅ | 🔴 | 🟢 |
| **6. Person_reID-densenet121** | ✅ | ✅ | 🟢 | ✅ | ✅ | ✅ |
| **7. Person_reID-hrnet18** | ✅ | ✅ | 🟢 | ✅ | ✅ | ✅ |
| **8. Person_reID-pcb** | ✅ | ✅ | 🟢 | ✅ | ✅ | ✅ |
| **9. examples-MNIST CNN** | ✅ | ✅ | ✅ | 🟢 | 🟡 | 🟢 |
| **10. examples-MNIST RNN** | ✅ | ✅ | ✅ | 🟢 | ❌ | 🟢 |
| **11. examples-MNIST FF** | ✅ | ✅ | ✅ | 🟢 | ❌ | 🟢 |
| **12. examples-Siamese** | ✅ | ✅ | ✅ | 🟢 | 🟡 | 🟢 |
| **可达到支持率** | **100%** | **83%** | **100%** | **92%** | **42%** | **92%** |

---

## 🎯 具体分析：易添加的参数

### 1️⃣ **Seed - 5个模型需要添加**

| 模型 | 当前状态 | 添加难度 | 原始行为 | 默认值策略 | 代码行数 |
|------|---------|---------|---------|-----------|---------|
| pytorch_resnet_cifar10 | ❌ | 🟢 极易 | 无seed，不可重复 | `None` | ~15行 |
| Person_reID (3个模型) | ❌ | 🟢 极易 | 无seed，不可重复 | `None` | ~15行 |
| bug-localization | ❌ | 🟢 极易 | 无seed，不可重复 | `None` | ~10行 |

**添加方式**：
```python
# 1. 添加argparse参数（1行）
parser.add_argument('--seed', type=int, default=None)

# 2. 设置seed逻辑（10-15行）
if args.seed is not None:
    torch.manual_seed(args.seed)
    np.random.seed(args.seed)
    random.seed(args.seed)
    torch.cuda.manual_seed_all(args.seed)
    torch.backends.cudnn.deterministic = True
    torch.backends.cudnn.benchmark = False
```

**默认值保证**：
- `default=None` → 不传参数时不设置seed → **完全保持原始随机行为** ✅

---

### 2️⃣ **Precision (fp16/bf16) - 9个模型需要添加/增强**

| 模型 | 当前状态 | 添加难度 | 原始行为 | 默认值策略 | 代码行数 |
|------|---------|---------|---------|-----------|---------|
| MRT-OAST | ❌ | 🟢 易 | fp32 | `None` | ~20行 |
| pytorch_resnet_cifar10 | ⚠️ 仅fp16 | 🟢 易 | fp32 | `None` (添加bf16) | ~10行 |
| examples (4个模型) | ❌ | 🟢 易 | fp32 | `None` | ~20行/模型 |

**添加方式**：
```python
# 1. 添加argparse参数（2行）
parser.add_argument('--fp16', action='store_true')
parser.add_argument('--bf16', action='store_true')

# 2. 混合精度训练（15-20行）
use_amp = False
amp_dtype = torch.float32

if args.fp16:
    use_amp = True
    amp_dtype = torch.float16
elif args.bf16:
    use_amp = True
    amp_dtype = torch.bfloat16

scaler = torch.cuda.amp.GradScaler(enabled=use_amp)

# 训练循环修改
if use_amp:
    with torch.cuda.amp.autocast(dtype=amp_dtype):
        output = model(data)
        loss = criterion(output, target)
    scaler.scale(loss).backward()
    scaler.step(optimizer)
    scaler.update()
else:
    # 原始fp32训练
    output = model(data)
    loss = criterion(output, target)
    loss.backward()
    optimizer.step()
```

**默认值保证**：
- `action='store_true'` → 不传`--fp16`或`--bf16`时 → `use_amp=False` → **保持fp32训练** ✅

---

### 3️⃣ **Weight Decay - 7个模型需要添加**

| 模型 | 当前状态 | 添加难度 | 原始值 | 默认值设置 | 代码行数 |
|------|---------|---------|-------|-----------|---------|
| MRT-OAST | ❌ | 🟢 极易 | 0 | `default=0` | ~5行 |
| VulBERTa-MLP | ❌ | 🟢 极易 | 0 | `default=0` | ~5行 |
| VulBERTa-CNN | ❌ | 🟢 极易 | 0 | `default=0` | ~5行 |
| examples (4个模型) | ❌ | 🟢 极易 | 0 | `default=0` | ~5行/模型 |

**添加方式**：
```python
# 1. 添加argparse参数（1行）
parser.add_argument('--weight_decay', type=float, default=0)  # ⚠️ 使用原始默认值！

# 2. 修改优化器（1-3行）
# 原始：optimizer = optim.Adam(model.parameters(), lr=args.lr)
# 修改为：
optimizer = optim.Adam(model.parameters(), lr=args.lr,
                       weight_decay=args.weight_decay)
```

**默认值保证**：
- `default=0` → 不传参数时使用0 → **与原始代码一致（Adam默认weight_decay=0）** ✅

**⚠️ 关键注意**：不同模型的原始weight_decay不同！
```python
# pytorch_resnet_cifar10: 原始是 1e-4
parser.add_argument('--weight_decay', type=float, default=1e-4)  # 不能改！

# Person_reID: 原始是 5e-4
parser.add_argument('--weight_decay', type=float, default=5e-4)  # 不能改！

# MRT-OAST, VulBERTa, examples: 原始是 0
parser.add_argument('--weight_decay', type=float, default=0)
```

---

## 🎯 优先级推荐：最小修改方案

### 阶段1: 超简单修改（1-2小时）

**只添加seed支持到5个模型**

| 模型 | 工作量 | 影响 |
|------|-------|------|
| pytorch_resnet_cifar10 | 15行代码 | 使训练可重复 |
| Person_reID (3个模型) | 15行代码 (共享一个train.py) | 使训练可重复 |
| bug-localization | 10行代码 | 使训练可重复 |

**效果**：
- ✅ 100%模型支持seed变异
- ✅ 默认值=None保证原始行为
- ✅ 无需修改模型架构
- ⚠️ 仅影响随机数生成，不改变模型本身

---

### 阶段2: 简单修改（半天）

**阶段1 + 添加weight_decay到7个模型**

| 模型 | 工作量 | 影响 |
|------|-------|------|
| MRT-OAST | 5行代码 | L2正则化可调 |
| VulBERTa (2个模型) | 5行代码×2 | L2正则化可调 |
| examples (4个模型) | 5行代码×4 | L2正则化可调 |

**效果**：
- ✅ 92%模型支持weight_decay变异
- ✅ 默认值=0保证原始行为（除了本就有weight_decay的）
- ✅ 无需修改模型架构

---

### 阶段3: 中等修改（1天）

**阶段2 + 添加precision支持**

| 模型 | 工作量 | 影响 |
|------|-------|------|
| MRT-OAST | 20行代码 | 支持fp16/bf16 |
| pytorch_resnet_cifar10 (添加bf16) | 10行代码 | 添加bf16选项 |
| examples (4个模型) | 20行代码×4 | 支持fp16/bf16 |

**效果**：
- ✅ 92%模型支持precision变异
- ✅ 默认值=None保证fp32训练
- ✅ 无需修改模型架构
- ⚠️ VulBERTa-MLP特例：原始用fp16，需特殊处理

---

## 🔴 不建议修改的参数

### 1. Dropout（困难且风险高）

| 模型 | 当前状态 | 为什么不建议 |
|------|---------|------------|
| VulBERTa (2个) | 固定在模型中 | 预训练模型内部dropout，修改需重新训练 |
| pytorch_resnet_cifar10 | 架构无dropout | ResNet设计无dropout层，添加改变架构 |
| examples-MNIST CNN | 硬编码 | 需要修改网络定义，重新验证性能 |
| bug-localization | sklearn限制 | MLPClassifier无dropout参数 |

**风险**：
- 🔴 修改模型架构 → 性能基准改变
- 🔴 需要大量测试确保模型仍能收敛
- 🔴 可能需要调整其他超参数

---

### 2. Learning Rate (sklearn模型)

| 模型 | 当前状态 | 为什么不支持 |
|------|---------|------------|
| bug-localization | sklearn限制 | MLPClassifier的learning_rate_init不在原始代码中 |

**可添加但有风险**：
```python
# sklearn的learning_rate_init可以设置
MLPClassifier(..., learning_rate_init=0.001)
# 但原始代码使用默认值，添加可能影响结果
```

---

## 📊 最终推荐配置

### 🎯 方案A: 最小改动（推荐用于快速启动）

**修改内容**：仅添加seed
- 5个模型添加seed支持
- 工作量：1-2小时
- 影响：最小（仅随机数）

**支持矩阵**：

| 超参数 | 支持模型数 | 支持率 |
|--------|-----------|-------|
| epochs | 12/12 | 100% |
| learning_rate | 10/12 | 83% |
| **seed** | **12/12** | **100%** ⬆️ |
| precision | 5/12 | 42% |
| dropout | 3/12 | 25% |
| weight_decay | 7/12 | 58% |

---

### 🎯 方案B: 平衡方案（推荐用于完整实验）

**修改内容**：seed + weight_decay + precision(部分)
- 5个模型添加seed
- 7个模型添加weight_decay
- 6个模型添加/增强precision
- 工作量：1天
- 影响：中等（不改模型架构）

**支持矩阵**：

| 超参数 | 支持模型数 | 支持率 |
|--------|-----------|-------|
| epochs | 12/12 | 100% |
| learning_rate | 10/12 | 83% |
| **seed** | **12/12** | **100%** ⬆️ |
| **precision** | **11/12** | **92%** ⬆️ |
| dropout | 3/12 | 25% |
| **weight_decay** | **11/12** | **92%** ⬆️ |

**不支持的组合**：
- bug-localization: learning_rate, precision, dropout (sklearn限制)
- ResNet: dropout (架构设计)
- MNIST RNN/FF: dropout (架构设计)

---

## ✅ 默认值保证总结表

| 超参数 | 默认值策略 | 保证原始行为方式 |
|--------|-----------|----------------|
| **epochs** | 使用原始值 | 已在代码中，不修改 ✅ |
| **learning_rate** | 使用原始值 | 已在代码中，不修改 ✅ |
| **seed** | `None` | `if args.seed is not None` 条件设置 ✅ |
| **precision** | `None` 或 `action='store_true'` | 不传参数时 `use_amp=False` ✅ |
| **dropout** | 使用原始值 | 已在代码中（如有），不修改 ✅ |
| **weight_decay** | 使用原始值（0或特定值） | 设置为原始优化器的值 ✅ |

---

## 📋 具体实现检查表

### ✅ 添加seed前的验证
- [ ] 检查原始代码是否有seed设置
- [ ] 确认使用 `default=None`
- [ ] 测试：不传`--seed` → 无seed设置日志
- [ ] 测试：传`--seed 42` → 看到seed设置日志
- [ ] 对比：两次不传seed的训练结果应不同（随机性）
- [ ] 对比：两次传相同seed的训练结果应相同（可重复）

### ✅ 添加weight_decay前的验证
- [ ] 查看原始优化器配置中的weight_decay值
- [ ] 确认默认值与原始值一致
- [ ] 测试：不传参数 → 使用默认weight_decay
- [ ] 测试：传不同weight_decay → 优化器使用新值
- [ ] 对比：默认值训练结果与原始baseline接近（<2%差异）

### ✅ 添加precision前的验证
- [ ] 确认原始代码使用fp32（除VulBERTa-MLP）
- [ ] VulBERTa-MLP特殊处理：默认fp16
- [ ] 测试：不传参数 → 无混合精度日志
- [ ] 测试：传`--fp16` → 看到autocast日志
- [ ] 对比：fp32训练显存占用 vs fp16显存占用（约2倍差异）

---

## 🎓 关键要点

### 1. **保证原始行为的三大原则**

✅ **原则1**: 新增参数使用 `None` 或 `False` 作为默认值
✅ **原则2**: 已有参数不修改其默认值
✅ **原则3**: 条件激活新功能（`if args.xxx is not None`）

### 2. **测试验证的三步法**

1️⃣ **Baseline测试**: 不传新参数，验证与原始训练一致
2️⃣ **功能测试**: 传新参数，验证新功能正常工作
3️⃣ **对比测试**: 对比性能差异在预期范围内

### 3. **特殊情况处理**

⚠️ **VulBERTa-MLP**: 唯一原始使用fp16的模型
⚠️ **Weight Decay**: 不同模型原始值差异大（0, 1e-5, 1e-4, 5e-4）
⚠️ **Seed**: 原始5个模型完全无seed设置

---

## 📊 最终推荐

### 🏆 推荐采用 **方案B（平衡方案）**

**理由**：
1. ✅ 92%的参数覆盖率（5个中的4个可变异）
2. ✅ 所有修改不改变模型架构
3. ✅ 工作量可控（1天内完成）
4. ✅ 默认值完全保证原始训练行为
5. ✅ 足够的超参数变异空间

**实施顺序**：
```
Day 1 上午: 添加seed到5个模型（2小时）
Day 1 下午: 添加weight_decay到7个模型（2小时）
Day 2 上午: 添加precision到6个模型（3小时）
Day 2 下午: 测试验证所有修改（2小时）
```

---

**文档版本**: 1.0
**最后更新**: 2025-11-05
**状态**: 待实施

**下一步**: 等待确认方案后开始代码修改
