# 超参数变异范围快速参考

**最后更新**: 2025-11-16 | **版本**: v4.1.0

本文档描述所有模型的超参数变异范围配置,基于 `mutation/models_config.json`。

---

## 📋 超参数变异范围总览

### MRT-OAST

| 参数 | 默认值 | 变异范围 | 分布方式 |
|------|--------|---------|---------|
| epochs | 10 | [5, 15] | uniform |
| learning_rate | 0.0001 | [0.00005, 0.00015] | log_uniform |
| dropout | 0.2 | [0.0, 0.3] | uniform |
| weight_decay | 0.0 | [0.00001, 0.001] | log_uniform |
| seed | 1334 | [0, 9999] | uniform |

**性能指标**: Accuracy, Precision, Recall, F1

---

### pytorch_resnet_cifar10

| 参数 | 默认值 | 变异范围 | 分布方式 |
|------|--------|---------|---------|
| epochs | 200 | [100, 300] | uniform |
| learning_rate | 0.1 | [0.05, 0.15] | log_uniform |
| weight_decay | 0.0001 | [0.00001, 0.001] | log_uniform |
| seed | null | [0, 9999] | uniform |

**模型**: resnet20, resnet32, resnet44, resnet56
**性能指标**: test_accuracy, best_val_accuracy

---

### VulBERTa

| 参数 | 默认值 | 变异范围 | 分布方式 |
|------|--------|---------|---------|
| epochs | 10 | [5, 20] | uniform |
| learning_rate | 0.00003 | [0.000015, 0.000045] | log_uniform |
| weight_decay | 0.0 | [0.00001, 0.001] | log_uniform |
| seed | 42 | [0, 9999] | uniform |

**模型**: mlp, cnn
**数据集**: d2a (固定)
**性能指标**: Accuracy, F1-score

**注意**: 预训练模型对学习率极敏感,范围较窄!

---

### Person_reID_baseline_pytorch

| 参数 | 默认值 | 变异范围 | 分布方式 |
|------|--------|---------|---------|
| epochs | 60 | [30, 90] | uniform |
| learning_rate | 0.05 | [0.025, 0.075] | log_uniform |
| dropout | 0.5 | [0.3, 0.6] | uniform |
| seed | null | [0, 9999] | uniform |

**模型**: densenet121, hrnet18, pcb
**性能指标**: Rank@1, Rank@5, mAP

**注意**: Dropout范围为[0.3, 0.6] (d-0.2到d+0.1)，基于边界测试验证，避免过高dropout导致性能劣化

---

### examples (PyTorch Examples)

| 参数 | 默认值 | 变异范围 | 分布方式 |
|------|--------|---------|---------|
| epochs | 10 | [5, 15] | uniform |
| learning_rate | 0.01 | [0.005, 0.02] | log_uniform |
| batch_size | 32 | [16, 128] | uniform |
| seed | 1 | [0, 9999] | uniform |

**模型**: mnist, mnist_rnn, mnist_ff, siamese, word_lm
**性能指标**: Final Test Accuracy, Test Loss

**注意**: MNIST简单,训练快,适合大量实验

---

### bug-localization-by-dnn-and-rvsm

| 参数 | 默认值 | 变异范围 | 分布方式 |
|------|--------|---------|---------|
| max_iter | 10000 | [1000, 20000] | log_uniform |
| kfold | 10 | [2, 10] | uniform |
| alpha | 0.00001 | [0.000005, 0.00002] | log_uniform |
| seed | 42 | [0, 9999] | uniform |

**性能指标**: Top-1, Top-5, MAP

---

## 🔬 分布方式说明

### Uniform (均匀分布)
- **适用**: epochs, dropout, batch_size, kfold, seed
- **特点**: 在范围内线性均匀采样
- **原因**: 这些参数对性能/能耗的影响是线性或接近线性的

### Log-Uniform (对数均匀分布)
- **适用**: learning_rate, weight_decay, alpha, max_iter
- **特点**: 在对数空间均匀采样,转换回线性空间
- **原因**: 这些参数对性能的影响是指数级的
- **示例**:
  - `[0.00005, 0.00015]` → log空间采样 → 可能得到 `0.00006, 0.0001, 0.00014`
  - 避免过度集中在高值区域

---

## 💡 范围设计原则

### 1. **保守范围** (当前策略)
- Learning Rate: ±50% (例如 0.1 → [0.05, 0.15])
- Epochs: ±50% (例如 200 → [100, 300])
- 确保大部分变异结果仍可收敛

### 2. **Weight Decay统一范围**
- 所有模型: [0.00001, 0.001]
- 对数采样,确保小值(接近0)也能被采样
- 覆盖从弱正则化到强正则化

### 3. **Dropout统一策略: d-0.2 到 d+0.1**
- MRT-OAST (d=0.2): [0.0, 0.3]
- Person_reID (d=0.5): [0.3, 0.6]
- 基于实验验证: dropout=0.7导致严重性能劣化(-3%~-8%)
- dropout=0.6经验证安全，性能变化<1%

### 4. **Seed范围**
- 统一: [0, 9999]
- 评估训练稳定性和随机性影响

---

## 📊 已完成的测试

### 范围测试 (run_20251116_153028)
验证新的超参数范围是否合理:
- Person_reID_baseline_pytorch (densenet121): dropout=0.5, epochs=60, lr=0.075
- pytorch_resnet_cifar10 (resnet20): epochs=200, lr=0.15, weight_decay=0.001
- MRT-OAST (default): dropout=0.2, epochs=10, lr=0.00015
- VulBERTa (mlp): epochs=10, lr=0.00003

**结论**: 所有配置成功收敛,范围合理

### 并行可行性测试V3 (run_20251116_184943)
测试11种GPU内存组合的并行训练可行性:
- 超低显存 + 超低显存: pytorch_resnet resnet20
- 超低显存 + 低显存: VulBERTa mlp
- 低显存 + 超低显存: examples mnist
- 低显存 + 低显存: MRT-OAST default
- 其他7种组合...

**结论**: 所有组合成功完成,并行训练机制稳定

---

## 🎯 使用建议

### 单参数研究（推荐新手）

```bash
# 只变异learning_rate,固定其他参数
python3 mutation.py -r pytorch_resnet_cifar10 -m resnet20 \
                    -mt learning_rate -n 5 -s 42
```

### 多参数变异

```bash
# epochs和learning_rate同时变异
python3 mutation.py -r pytorch_resnet_cifar10 -m resnet20 \
                    -mt epochs,learning_rate -n 10 -s 42
```

### 全参数随机探索

```bash
# 变异所有支持的参数
python3 mutation.py -r VulBERTa -m mlp -mt all -n 20
```

### 批量实验（推荐）

```bash
# 使用配置文件
sudo python3 mutation.py -ec settings/default.json
```

---

## ⚠️ 重要注意事项

### 1. 默认值排除
框架会自动排除默认值,确保变异结果不等于默认配置。

### 2. 重复检查
6位小数精度的唯一性检查,避免重复实验。

### 3. Learning Rate范围
- VulBERTa: 极窄范围 [0.000015, 0.000045],预训练模型敏感
- 其他模型: 相对宽松,但仍保守(±50%左右)

### 4. Weight Decay零值
- 部分模型默认值为0.0
- 变异时使用对数分布,从0.00001开始采样
- 如果需要测试"无正则化",需手动设置0.0

---

## 📚 相关文档

- [变异机制详解](MUTATION_MECHANISMS_DETAILED.md) - 算法实现细节
- [使用示例](USAGE_EXAMPLES.md) - 更多命令示例
- [实验配置指南](SETTINGS_CONFIGURATION_GUIDE.md) - 配置文件编写

---

**维护者**: Green
**项目**: Mutation-Based Training Energy Profiler
**配置文件**: mutation/models_config.json
