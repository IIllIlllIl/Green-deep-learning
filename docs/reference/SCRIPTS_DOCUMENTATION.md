# Scripts 目录文档

**创建日期**: 2025-12-08
**版本**: v4.7.2
**状态**: ✅ 当前有效

本文档记录 `scripts/` 目录中所有脚本的用途、功能和使用场景，便于维护和避免重复开发。

---

## 📁 目录结构

```
scripts/
├── aggregate_csvs.py                      # CSV数据聚合工具
├── analyze_baseline.py                    # 基线测试分析
├── analyze_experiments.py                 # 统一实验分析工具
├── analyze_stage_configs.py               # Stage配置分析
├── cleanup_docs_and_backups.sh           # 文档和备份清理（v4.7.2新增）
├── cleanup_orphan_processes.sh            # GPU残留进程清理
├── download_pretrained_models.py          # 预训练模型下载
├── fix_stage_configs.py                   # Stage配置修复（v4.7.1）
├── generate_mutation_config.py            # 变异配置生成器
├── run_sequential_experiments.sh          # 顺序执行实验
├── validate_mutation_config.py            # 变异配置验证
└── verify_stage_configs.py               # Stage配置验证（v4.7.2新增）
```

---

## 📊 脚本分类

### 1. 数据分析工具（4个）

#### 1.1 `aggregate_csvs.py`
**功能**: 聚合多个实验会话的CSV数据

**用途**:
- 合并多个 `run_YYYYMMDD_HHMMSS/summary.csv` 到 `summary_all.csv`
- 自动去重，防止重复导入
- 支持递归扫描results目录

**使用场景**:
- 实验完成后聚合结果数据
- 重建 `summary_all.csv` 文件

**文件大小**: 402行
**依赖**: csv, json, pathlib
**状态**: ✅ 完成且稳定

---

#### 1.2 `analyze_baseline.py`
**功能**: 分析基线测试结果

**用途**:
- 分析默认配置的基线性能
- 统计能耗、训练时间、性能指标
- 生成基线对比报告

**使用场景**:
- 验证基线实验结果
- 为变异实验提供参考基准

**文件大小**: 249行
**依赖**: csv, statistics
**状态**: ✅ 完成且稳定

---

#### 1.3 `analyze_experiments.py`
**功能**: 统一实验分析工具（支持CSV和JSON源）

**用途**:
- 从 `summary_all.csv` 或实验JSON配置分析数据
- 生成详细统计报告
- 支持多数据源分析

**使用场景**:
- 灵活的实验数据分析
- 生成实验统计报告

**文件大小**: 419行
**依赖**: csv, json, argparse
**状态**: ✅ 完成且稳定

---

#### 1.4 `analyze_stage_configs.py`
**功能**: 分析Stage3/4配置文件

**用途**:
- 分析Stage3/4合并配置的详细信息
- 统计实验数量、预估时间
- 验证配置正确性

**使用场景**:
- Stage3/4配置设计阶段使用
- **历史作用**: 仅用于Stage3/4设计阶段

**文件大小**: 108行
**依赖**: json
**状态**: ⚠️ 历史工具（Stage3/4已完成，可归档）

---

### 2. 配置管理工具（5个）

#### 2.1 `fix_stage_configs.py` ⭐
**功能**: 修复Stage7-13配置文件的多参数混合变异问题

**用途**:
- 将多参数配置项拆分为单参数配置项
- 自动备份原配置文件（.json.bak）
- 更新estimated_experiments和version字段
- 支持dry-run模式预览更改

**影响**: 修复了19个配置项 → 62个配置项（v4.7.1）

**使用方法**:
```bash
# 预览更改（不保存）
python3 scripts/fix_stage_configs.py --dry-run

# 实际修复
python3 scripts/fix_stage_configs.py
```

**文件大小**: 254行
**依赖**: json, pathlib
**状态**: ✅ 关键工具（v4.7.1核心修复）

---

#### 2.2 `verify_stage_configs.py` ⭐
**功能**: 验证Stage配置文件的runs_per_config定义位置

**用途**:
- 检查并行模式配置的runs_per_config定义（外层/foreground/全局）
- 验证配置符合v4.7.2修复后的代码兼容性
- 自动化配置审查

**验证范围**: Stage11, Stage12, Stage13

**使用方法**:
```bash
python3 scripts/verify_stage_configs.py
```

**输出**: 详细验证报告 + 通过/失败状态

**文件大小**: 150行
**依赖**: json, pathlib
**状态**: ✅ 关键工具（v4.7.2新增）

---

#### 2.3 `generate_mutation_config.py`
**功能**: 变异实验配置生成器

**用途**:
- 为11个模型生成完整变异配置
- 每个超参数变异3次（低、中、高）
- 生成顺序+并行训练配置
- 估算实验时间

**使用场景**:
- 生成大规模变异实验配置
- 自动化配置创建

**使用方法**:
```bash
# 查看统计信息
python3 tools/config_management/generate_mutation_config.py --stats-only

# 生成配置文件
python3 tools/config_management/generate_mutation_config.py --output settings/mutation_all.json
```

**文件大小**: 465行
**依赖**: json, argparse
**状态**: ✅ 完成且稳定

---

#### 2.4 `validate_mutation_config.py`
**功能**: 验证mutation配置JSON是否能正确调用变异方法

**用途**:
- 验证JSON格式正确性
- 验证mutation模式配置
- 测试generate_mutations()调用
- 检查配置与models_config.json兼容性

**使用场景**:
- 配置文件创建后验证
- 运行前配置检查

**使用方法**:
```bash
python3 tools/config_management/validate_mutation_config.py
```

**文件大小**: 238行
**依赖**: json, mutation.runner, mutation.hyperparams
**状态**: ✅ 完成且稳定

---

#### 2.5 `cleanup_docs_and_backups.sh` ⭐
**功能**: 归档过时文档并删除不必要的备份文件

**用途**:
- 归档过时文档到 `docs/archived/YYYY-MM-DD_*`
- 删除包含错误的配置备份文件
- 创建归档说明文档（README_ARCHIVE.md）
- 保持项目清洁和可维护

**执行结果（2025-12-08）**:
- 归档10个文档
- 删除9个错误备份
- docs文件减少35.7%

**使用方法**:
```bash
bash scripts/cleanup_docs_and_backups.sh
```

**文件大小**: 195行
**依赖**: bash
**状态**: ✅ 维护工具（v4.7.2新增）

---

### 3. 系统维护工具（2个）

#### 3.1 `cleanup_orphan_processes.sh`
**功能**: 清理GPU残留进程

**用途**:
- 清理2x mutation run遗留的孤儿进程
- 释放GPU内存
- 支持优雅终止（SIGTERM）和强制终止（SIGKILL）
- 显示清理前后GPU状态

**使用场景**:
- 实验异常退出后清理GPU
- 手动释放GPU资源

**使用方法**:
```bash
sudo bash scripts/cleanup_orphan_processes.sh
```

**注意**: 脚本中硬编码了特定进程ID（1970647, 1970645），需根据实际情况修改

**文件大小**: 116行
**依赖**: bash, nvidia-smi
**状态**: ⚠️ 特定任务脚本（可能需要更新或归档）

---

#### 3.2 `download_pretrained_models.py`
**功能**: 预下载HuggingFace模型权重

**用途**:
- 下载所有需要的预训练模型权重到本地缓存
- 支持timm模型（HRNet）和torchvision模型（ResNet, DenseNet）
- 启用离线训练实验
- 验证离线加载功能

**下载模型**:
- `hrnet_w18` (timm)
- `resnet50` (torchvision)
- `densenet121` (torchvision)

**使用场景**:
- 实验前准备环境
- 离线环境配置

**使用方法**:
```bash
python3 scripts/download_pretrained_models.py
```

**文件大小**: 267行
**依赖**: timm, torchvision, pathlib
**状态**: ✅ 完成且稳定

---

#### 3.3 `run_sequential_experiments.sh`
**功能**: 顺序运行多个实验配置

**用途**:
- 自动化多个实验配置的顺序执行
- 记录实验日志
- 显示开始/结束时间和退出代码
- 支持实验间等待时间

**使用场景**:
- 批量执行多个stage配置
- 无人值守实验运行

**使用方法**:
```bash
# 编辑脚本，修改配置文件列表
bash scripts/run_sequential_experiments.sh
```

**注意**: 脚本中硬编码了特定配置文件，需根据实际需求修改

**文件大小**: 62行
**依赖**: bash
**状态**: ⚠️ 模板脚本（需根据实际需求修改）

---

## 📊 脚本重要性分级

### ⭐⭐⭐ 关键工具（核心功能，频繁使用）
- `fix_stage_configs.py` - Stage7-13配置修复（v4.7.1）
- `verify_stage_configs.py` - 配置验证（v4.7.2）
- `cleanup_docs_and_backups.sh` - 项目清理维护（v4.7.2）
- `aggregate_csvs.py` - 数据聚合

### ⭐⭐ 重要工具（常用分析和配置）
- `analyze_experiments.py` - 统一分析工具
- `generate_mutation_config.py` - 配置生成器
- `validate_mutation_config.py` - 配置验证
- `download_pretrained_models.py` - 环境准备

### ⭐ 辅助工具（特定场景使用）
- `analyze_baseline.py` - 基线分析
- `run_sequential_experiments.sh` - 批量执行
- `cleanup_orphan_processes.sh` - GPU清理

### ⚠️ 历史工具（已完成任务，可归档）
- `analyze_stage_configs.py` - Stage3/4专用（已完成）

---

## 🔍 重复功能检查

### 实验分析功能
- **`analyze_experiments.py`**: 通用分析工具（CSV/JSON源）✅
- **`analyze_baseline.py`**: 基线测试专用分析 ✅
- **`analyze_stage_configs.py`**: Stage3/4配置分析 ⚠️ （已完成，可归档）

**结论**: `analyze_experiments.py` 是主分析工具，`analyze_baseline.py` 用于特定基线分析，无重复。`analyze_stage_configs.py` 已完成历史任务，建议归档。

### 配置验证功能
- **`verify_stage_configs.py`**: 验证runs_per_config定义位置（v4.7.2）✅
- **`validate_mutation_config.py`**: 验证mutation配置格式和可执行性 ✅

**结论**: 两者功能不同，无重复。前者验证配置参数定义位置，后者验证配置格式和运行兼容性。

---

## 📝 维护建议

### 1. 可归档脚本
建议归档以下脚本到 `scripts/archived/`:

#### `analyze_stage_configs.py`
- **原因**: 仅用于Stage3/4设计阶段，Stage3/4已完成（2025-12-05）
- **归档位置**: `scripts/archived/analyze_stage_configs.py`
- **保留README**: 说明历史用途和归档原因

#### `cleanup_orphan_processes.sh`
- **原因**: 硬编码特定进程ID（1970647, 1970645），已完成特定清理任务
- **建议**:
  - 如果仍需使用，重构为通用版本（接受进程ID参数）
  - 如果不再需要，归档到 `scripts/archived/`

### 2. 需更新脚本

#### `run_sequential_experiments.sh`
- **问题**: 硬编码特定配置文件路径
- **建议**: 重构为接受配置文件列表参数
```bash
# 改进版本
bash scripts/run_sequential_experiments.sh \
  settings/stage11_parallel_hrnet18.json \
  settings/stage12_parallel_pcb.json
```

### 3. 脚本命名规范

建议统一使用以下命名规范：
- **分析脚本**: `analyze_*.py`
- **修复脚本**: `fix_*.py`
- **验证脚本**: `verify_*.py` 或 `validate_*.py`
- **生成脚本**: `generate_*.py`
- **清理脚本**: `cleanup_*.sh`
- **运行脚本**: `run_*.sh`

当前所有脚本已符合此规范 ✅

---

## 🎯 使用建议

### 日常工作流程

#### 实验前准备
1. **下载预训练模型**:
   ```bash
   python3 scripts/download_pretrained_models.py
   ```

2. **生成或修改配置**:
   ```bash
   python3 tools/config_management/generate_mutation_config.py
   ```

3. **验证配置**:
   ```bash
   python3 tools/config_management/validate_mutation_config.py
   python3 scripts/verify_stage_configs.py
   ```

#### 实验执行
4. **运行实验**:
   ```bash
   sudo -E python3 mutation.py -ec settings/stage11_parallel_hrnet18.json
   ```

5. **聚合结果**:
   ```bash
   python3 scripts/aggregate_csvs.py
   ```

#### 实验后分析
6. **分析数据**:
   ```bash
   python3 scripts/analyze_experiments.py
   ```

#### 定期维护
7. **清理文档和备份**:
   ```bash
   bash scripts/cleanup_docs_and_backups.sh
   ```

8. **清理GPU进程**（如需要）:
   ```bash
   sudo bash scripts/cleanup_orphan_processes.sh
   ```

---

## 📚 相关文档

- [项目主文档](../CLAUDE.md) - Claude助手指南
- [配置最佳实践](../docs/JSON_CONFIG_BEST_PRACTICES.md) - JSON配置规范
- [Stage7-13配置修复报告](../docs/results_reports/STAGE7_13_CONFIG_BUG_ANALYSIS.md)
- [Stage配置验证报告](../docs/results_reports/STAGE_CONFIG_VERIFICATION_REPORT.md)

---

**维护者**: Green + Claude
**创建日期**: 2025-12-08
**最后更新**: 2025-12-08
**版本**: v4.7.2
**状态**: ✅ 当前有效
