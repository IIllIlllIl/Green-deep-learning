# 当前Mutation脚本 vs 推荐设计 - 差异分析

**分析时间**: 2025-11-15 17:00
**对比对象**:
- 当前实现: `mutation/hyperparams.py` + `mutation/models_config.json`
- 推荐设计: 基于boundary_test_v2结果的优化范围

---

## 1. 核心差异总览

| 维度 | 当前实现 | 推荐设计 | 差异程度 |
|------|---------|---------|---------|
| **变异策略** | 随机采样（连续分布） | 边界测试（离散点） | ⚠️ 重大 |
| **Epochs范围** | 多样化（按模型） | 建议固定 | ⚠️ 重大 |
| **LR范围** | 较激进 | 保守 | ⚠️ 中等 |
| **Weight Decay** | 30%零概率+随机 | 倍数或混合 | ⚠️ 中等 |
| **Dropout范围** | 较窄 | 较宽或偏移量 | ⚠️ 轻微 |
| **Seed范围** | 一致 | 一致 | ✅ 相同 |

---

## 2. 逐个超参数详细对比

### 2.1 Epochs

#### 当前实现

```python
# mutation/hyperparams.py 行105-106
# Epochs: Log-uniform distribution [default×0.5, default×2.0]

# models_config.json 示例:
"DenseNet121": {
    "epochs": {
        "default": 60,
        "range": [30, 90],  # [0.5×, 1.5×]
        "distribution": "log_uniform"
    }
}

"ResNet20": {
    "epochs": {
        "default": 200,
        "range": [100, 300],  # [0.5×, 1.5×]
        "distribution": "log_uniform"
    }
}

"MRT-OAST": {
    "epochs": {
        "default": 10,
        "range": [5, 15],  # [0.5×, 1.5×]
        "distribution": "log_uniform"
    }
}
```

**变异方式**: 连续随机采样
**示例输出**: 73, 45, 88（任意值）

#### 推荐设计

```json
{
  "epochs": {
    "type": "fixed",
    "value": "default",
    "rationale": "保持训练充分性一致，能耗可比"
  }
}
```

**变异方式**: 不变异

#### 差异分析

| 方面 | 当前实现 | 推荐设计 | 影响 |
|------|---------|---------|------|
| **范围** | [0.5×, 1.5×] | 固定 | ⚠️ 重大 |
| **上界** | 1.5× (vs 推荐2×) | 固定 | - |
| **采样** | 连续随机 | - | - |
| **能耗归因** | ❌ 困难 | ✅ 清晰 | 重要 |

**建议**: ✅ **采用推荐设计（固定Epochs）**
- 当前实现会导致能耗差异无法归因
- 训练时间直接影响能耗（epochs×2 → 能耗×2）

---

### 2.2 Learning Rate

#### 当前实现

```python
# mutation/hyperparams.py 行106-107
# Learning Rate: Log-uniform distribution [default×0.1, default×10.0]

"DenseNet121": {
    "learning_rate": {
        "default": 0.05,
        "range": [0.01, 0.25],  # [0.2×, 5×]
        "distribution": "log_uniform"
    }
}

"ResNet20": {
    "learning_rate": {
        "default": 0.1,
        "range": [0.02, 0.5],  # [0.2×, 5×]
        "distribution": "log_uniform"
    }
}

"MRT-OAST": {
    "learning_rate": {
        "default": 0.0001,
        "range": [0.00002, 0.0005],  # [0.2×, 5×]
        "distribution": "log_uniform"
    }
}
```

**变异方式**: 连续log-uniform采样
**示例输出**: 0.123, 0.0347, 0.198（任意值）

#### 推荐设计

```json
{
  "learning_rate": {
    "formula": "LR_default × multiplier",
    "range": "[0.5×, 2.0×]",
    "test_points": [0.5, 1.0, 2.0]
  }
}
```

**变异方式**: 离散倍数测试

#### 差异分析

| 方面 | 当前实现 | 推荐设计 | 差异说明 |
|------|---------|---------|---------|
| **下界** | 0.2× | **0.5×** | 当前更激进 |
| **上界** | **5×** | 2× | ⚠️ **当前风险极高！** |
| **采样方式** | 连续随机 | 离散点 | 不同方法论 |

**实测数据验证**:

| 模型 | 当前上界5× | 实测4× | 推荐2× |
|------|-----------|--------|--------|
| DenseNet121 | **0.25 (5×)** | 0.2 (4×): **崩溃** ❌ | 0.1 (2×): 需验证 |
| MRT-OAST | 0.0005 (5×) | 0.0004 (4×): **最佳** ✅ | 0.0002 (2×): 需验证 |
| ResNet20 | **0.5 (5×)** | 0.4 (4×): -0.85% ✅ | 0.2 (2×): 需验证 |

**关键发现**: ❌ **当前5×上界对DenseNet极度危险**
- DenseNet在4×已崩溃，5×必然失败
- 当前范围`[0.01, 0.25]`包含灾难性区域

**建议**: ⚠️ **必须修改当前实现**
```json
{
  "DenseNet121": {
    "learning_rate": {
      "range": [0.025, 0.1]  // [0.5×, 2×] 而非 [0.01, 0.25]
    }
  }
}
```

---

### 2.3 Weight Decay

#### 当前实现

```python
# mutation/hyperparams.py 行107-108
# Weight Decay: 30% zero + 70% log-uniform [0.0, default×100]

"ResNet20": {
    "weight_decay": {
        "default": 0.0001,
        "range": [0.00001, 0.01],  # [0.1×, 100×]
        "distribution": "log_uniform",
        "zero_probability": 0.3
    }
}

"MRT-OAST": {
    "weight_decay": {
        "default": 0.0,
        "range": [0.00001, 0.01],
        "distribution": "log_uniform",
        "zero_probability": 0.3
    }
}
```

**变异方式**: 30%概率为0，70%概率从[min, max]采样
**特殊处理**: default=0时仍可采样非零值

#### 推荐设计

```json
{
  "weight_decay": {
    "type": "adaptive",
    "if_nonzero": "WD_default × multiplier, range [0.1×, 10×]",
    "if_zero": "absolute values [0.0, 0.001, 0.01]",
    "test_points": [0.1, 1.0, 10.0] or [0, 0.001, 0.01]
  }
}
```

**变异方式**: 条件判断 + 离散倍数或绝对值

#### 差异分析

| 方面 | 当前实现 | 推荐设计 | 差异 |
|------|---------|---------|------|
| **上界** | **100×** | 10× | 当前过激进 |
| **零概率** | 30% | 条件处理 | 方法不同 |
| **default=0** | 仍可非零 | 绝对值范围 | 一致（都允许引入WD） |
| **采样** | 连续 | 离散 | 方法论差异 |

**示例对比**:

ResNet20 (default=0.0001):
- 当前: 30%→0, 70%→[0.00001, 0.01]随机
  - 可能值: 0, 0.00003, 0.00087, 0.0045, ...
- 推荐: [0.00001, 0.0001, 0.001]
  - 固定3个点

**上界100×的风险**:
```
ResNet20 default=0.0001
上界: 0.0001 × 100 = 0.01

问题: 0.01是非常大的weight decay
- 通常范围: 1e-5 ~ 1e-3
- 0.01可能严重损害性能（过度正则化）
```

**建议**: ⚠️ **降低上界到10×**

---

### 2.4 Dropout

#### 当前实现

```python
# mutation/hyperparams.py 行108-109
# Dropout: Uniform distribution [0.0, 0.7]

"DenseNet121": {
    "dropout": {
        "default": 0.5,
        "range": [0.0, 0.5],  // 注意：上界=default
        "distribution": "uniform"
    }
}

"MRT-OAST": {
    "dropout": {
        "default": 0.2,
        "range": [0.0, 0.5],
        "distribution": "uniform"
    }
}
```

**变异方式**: 连续均匀采样
**示例输出**: 0.123, 0.456, 0.089（任意值）

#### 推荐设计（两个选项）

**选项A: 偏移量**
```json
{
  "dropout": {
    "formula": "clip(default + offset, 0, 1)",
    "range": "[-0.3, +0.2]"
  }
}
```

**选项B: 绝对值**
```json
{
  "dropout": {
    "formula": "Dropout ∈ [0.0, 0.7]",
    "test_points": [0.0, 0.35, 0.7]
  }
}
```

#### 差异分析

| 方面 | 当前实现 | 推荐A（偏移） | 推荐B（绝对） |
|------|---------|-------------|-------------|
| **DenseNet上界** | 0.5 (=default) | 0.7 (0.5+0.2) | 0.7 |
| **MRT上界** | 0.5 (>default) | 0.4 (0.2+0.2) | 0.7 |
| **ResNet (d=0)** | N/A | 0.2 (0+0.2) | 0.7 (引入dropout) |
| **采样** | 连续 | 离散 | 离散 |

**实测验证**:

| 模型 | Default | 测试过的值 | 当前上界 | 推荐上界 |
|------|---------|-----------|---------|---------|
| DenseNet | 0.5 | 0.0(+2.4%), 0.4(+0.2%) | 0.5 | 0.7 (需验证) |
| MRT | 0.2 | 0.0(+5.81%) | 0.5 | 0.4或0.7 |

**关键问题**:
- ❌ 当前DenseNet上界0.5无法探索更高dropout（boundary_test已测0.0和0.4）
- ✅ 推荐上界0.7可以验证是否有上界风险

**建议**: ⚠️ **扩大DenseNet的dropout上界**
```json
{
  "DenseNet121": {
    "dropout": {
      "range": [0.0, 0.7]  // 而非 [0.0, 0.5]
    }
  }
}
```

---

### 2.5 Seed

#### 当前实现

```python
# mutation/hyperparams.py 行109
# Seed: Uniform integer [0, 9999]

"所有模型": {
    "seed": {
        "default": 42 或 null,
        "range": [0, 9999],
        "distribution": "uniform"
    }
}
```

**变异方式**: 连续随机整数

#### 推荐设计

```json
{
  "seed": {
    "values": [0, 42, 9999],
    "usage": "每配置重复3次或单独分析"
  }
}
```

**变异方式**: 离散固定值

#### 差异分析

| 方面 | 当前实现 | 推荐设计 |
|------|---------|---------|
| **范围** | [0, 9999] | [0, 9999] ✅ |
| **采样** | 任意整数 | 3个固定值 |
| **用途** | 随机化 | 重复实验 |

**一致性**: ✅ **范围相同**

**方法论差异**:
- 当前: 每次实验随机seed（不可重复）
- 推荐: 固定几个seed值（可重复验证）

---

## 3. 变异策略的根本差异

### 3.1 当前实现：连续随机采样

```python
# hyperparams.py
def mutate_hyperparameter(param_config, ...):
    if distribution == "log_uniform":
        log_min = math.log(min_val)
        log_max = math.log(max_val)
        log_value = random.uniform(log_min, log_max)
        return math.exp(log_value)  # 连续值
```

**特点**:
- ✅ 探索完整参数空间
- ✅ 可能发现意外最优点
- ❌ 结果不可重复（每次随机）
- ❌ 难以系统分析（无离散对比）

**适用场景**: 黑盒优化、AutoML

---

### 3.2 推荐设计：边界离散测试

```python
# 推荐方法
def mutate_hyperparameter(param, default, strategy):
    if strategy == "multiplier":
        return [default * 0.5, default * 1.0, default * 2.0]
```

**特点**:
- ✅ 结果可重复
- ✅ 易于对比分析
- ✅ 快速验证安全范围
- ❌ 可能错过最优点

**适用场景**: 能耗研究、安全范围验证

---

## 4. 配置文件对比

### 4.1 当前实现配置示例

```json
{
  "DenseNet121": {
    "epochs": {"range": [30, 90], "distribution": "log_uniform"},
    "learning_rate": {"range": [0.01, 0.25], "distribution": "log_uniform"},
    "dropout": {"range": [0.0, 0.5], "distribution": "uniform"}
  }
}
```

**运行示例**:
```bash
python mutation.py -r Person_reID_baseline_pytorch \
    -m densenet121 \
    --mutate epochs,learning_rate,dropout \
    --runs 10
```

**输出**: 10个随机配置，每个都不同
```
Mutation 1: epochs=73, lr=0.0347, dropout=0.231
Mutation 2: epochs=45, lr=0.1892, dropout=0.412
Mutation 3: epochs=88, lr=0.0123, dropout=0.089
...
```

---

### 4.2 推荐设计配置示例

```json
{
  "DenseNet121": {
    "epochs": {"type": "fixed", "value": 60},
    "learning_rate": {"type": "multiplier", "values": [0.5, 1.0, 2.0]},
    "dropout": {"type": "offset", "values": [-0.3, 0.0, 0.2]}
  }
}
```

**运行示例**:
```bash
python mutation.py -r Person_reID_baseline_pytorch \
    -m densenet121 \
    --strategy boundary_test \
    --params learning_rate,dropout
```

**输出**: 6个固定配置（3×2组合）
```
Config 1: epochs=60, lr=0.025 (0.5×), dropout=0.2 (-0.3)
Config 2: epochs=60, lr=0.025 (0.5×), dropout=0.5 (0.0)
Config 3: epochs=60, lr=0.025 (0.5×), dropout=0.7 (+0.2)
Config 4: epochs=60, lr=0.05 (1.0×), dropout=0.2
Config 5: epochs=60, lr=0.1 (2.0×), dropout=0.2
Config 6: epochs=60, lr=0.1 (2.0×), dropout=0.7
```

---

## 5. 实测数据对照

### 5.1 当前范围会遇到的问题

基于boundary_test_v2结果：

| 模型 | 超参数 | 当前范围 | 风险区域 | 实测结果 |
|------|--------|---------|---------|---------|
| DenseNet | LR | [0.01, 0.25] | **0.2~0.25** | 0.2→崩溃(-72.8%) ❌ |
| DenseNet | Epochs | [30, 90] | 30 | 未测试，可能欠拟合 |
| ResNet20 | WD | [0.00001, 0.01] | **0.005~0.01** | 未测试，可能过正则化 |
| MRT-OAST | LR | [0.00002, 0.0005] | ✅ | 0.0004最佳(+5.89%) |

**估算风险**:
- DenseNet LR范围中约 **20%** 会导致崩溃（0.2-0.25 / 0.01-0.25）
- ResNet WD范围中约 **50%** 可能过度正则化（0.005-0.01 / 0.00001-0.01）

---

### 5.2 推荐范围的安全性

| 模型 | 超参数 | 推荐范围 | 风险评估 |
|------|--------|---------|---------|
| DenseNet | LR | [0.025, 0.1] | ✅ 安全（内插-2%） |
| DenseNet | Epochs | 60 (固定) | ✅ 充分 |
| ResNet20 | WD | [0.00001, 0.001] | ✅ 合理 |
| MRT-OAST | LR | [0.00005, 0.0002] | ⚠️ 未达最佳（需2×上界） |

---

## 6. 总结与建议

### 6.1 关键差异

| 维度 | 当前实现 | 推荐设计 | 建议 |
|------|---------|---------|------|
| **方法论** | 连续随机采样 | 离散边界测试 | 看研究目标 |
| **LR上界** | 5× (危险) | 2× (安全) | ✅ **必须修改** |
| **WD上界** | 100× (过激) | 10× (合理) | ✅ **必须修改** |
| **Epochs** | 变异 | 固定 | ⚠️ **强烈建议固定** |
| **Dropout上界** | 0.5 (DenseNet) | 0.7 | ⚠️ **建议扩大** |

### 6.2 立即需要修改的配置

```json
{
  "Person_reID_baseline_pytorch": {
    "epochs": {
      "range": [60, 60]  // 固定，不变异
    },
    "learning_rate": {
      "range": [0.025, 0.1]  // [0.5×, 2×] 而非 [0.01, 0.25]
    },
    "dropout": {
      "range": [0.0, 0.7]  // 扩大上界
    }
  },

  "pytorch_resnet_cifar10": {
    "epochs": {
      "range": [200, 200]  // 固定
    },
    "learning_rate": {
      "range": [0.05, 0.2]  // [0.5×, 2×] 而非 [0.02, 0.5]
    },
    "weight_decay": {
      "range": [0.00001, 0.001]  // [0.1×, 10×] 而非 [0.00001, 0.01]
    }
  },

  "MRT-OAST": {
    "epochs": {
      "range": [10, 10]  // 固定
    },
    "learning_rate": {
      "range": [0.00005, 0.0002]  // [0.5×, 2×] 保守
    }
  }
}
```

### 6.3 实现路径

#### 选项1: 保持当前连续采样，修正范围（快速）

**优点**: 最小改动
**缺点**: 仍是随机采样，难以重复

```python
# 只需修改 models_config.json
# 按上述建议调整各模型的range
```

#### 选项2: 添加离散边界测试模式（推荐）

**优点**: 兼容两种方法
**缺点**: 需要代码改动

```python
# hyperparams.py 添加新函数
def boundary_test_mutation(param_config, param_name):
    """离散边界测试"""
    default = param_config["default"]
    if param_name == "learning_rate":
        return [default * 0.5, default, default * 2.0]
    elif param_name == "weight_decay":
        if default > 0:
            return [default * 0.1, default, default * 10]
        else:
            return [0.0, 0.001, 0.01]
    # ...
```

---

**报告版本**: 1.0
**生成时间**: 2025-11-15 17:00
**关键建议**:
1. ❗ **立即修正LR上界** - 避免DenseNet崩溃
2. ⚠️ **固定Epochs** - 保证能耗可比性
3. ⚠️ **降低WD上界** - 避免过度正则化
4. 📊 **考虑添加边界测试模式** - 提高实验可重复性
