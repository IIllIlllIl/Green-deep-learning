========================================
全量测试运行 - 快速参考
========================================

📁 配置文件位置
  settings/full_test_run.json

📋 将训练的模型 (6个)
  1. examples/mnist_cnn (10 epochs) ~10分钟
  2. MRT-OAST/default (10 epochs) ~30分钟
  3. bug-localization-by-dnn-and-rvsm/default (10 epochs) ~30分钟
  4. VulBERTa/mlp (10 epochs) ~1小时
  5. Person_reID_baseline_pytorch/densenet121 (60 epochs) ~4小时
  6. pytorch_resnet_cifar10/resnet20 (200 epochs) ~12小时

⏱️  预估总时间: 约12-18小时

========================================
🚀 运行命令 (在 screen -r test 中执行)
========================================

方法1 (推荐) - 使用便捷脚本:
  screen -r test
  cd /home/green/energy_dl/nightly
  sudo ./scripts/run_full_test.sh

方法2 - 直接运行:
  screen -r test
  cd /home/green/energy_dl/nightly
  sudo python3 mutation.py --experiment-config settings/full_test_run.json

========================================
📊 监控进度
========================================

查看screen会话:
  screen -r test
  (按 Ctrl+A 然后 D 断开连接)

查看最新结果:
  ls -lht results/*.json | head -10

检查是否在运行:
  ps aux | grep mutation.py

========================================
📖 详细文档
========================================

完整指南:
  docs/full_test_run_guide.md

包含:
  - 详细的测试说明
  - 监控方法
  - 故障排除
  - 结果分析方法

========================================
✅ 验证内容
========================================

此测试验证:
  ✓ 所有仓库的训练脚本功能正常
  ✓ 超参数传递正确
  ✓ 能耗监控工作正常
  ✓ 性能指标提取正确
  ✓ 自动重试机制有效
  ✓ 结果保存格式正确

========================================
