# 项目重组快速指南

**日期**: 2026-01-05
**预计时间**: 5-10分钟
**风险等级**: 低（已自动备份）

---

## 🎯 重组目标

- ✅ **数据文件上浮** - `results/raw_data.csv` → `data/raw_data.csv`
- ✅ **脚本分类整理** - `scripts/` → `tools/data_management/` + `tools/config_management/`
- ✅ **历史数据归档** - `results/run_*/` → `archives/runs/` (释放~1.8GB空间)
- ✅ **目录结构清晰** - 活跃文件更靠近根目录

---

## 🚀 三步执行

### 步骤1: 预览变更（Dry Run）

```bash
cd /home/green/energy_dl/nightly

# 查看重组方案
cat docs/RESTRUCTURE_PLAN_20260105.md

# 预览路径更新（不实际修改）
python3 update_paths.py
```

### 步骤2: 执行重组

```bash
# 执行重组脚本（会自动备份）
./restructure_project.sh
```

**脚本会自动完成**：
- ✅ 创建完整备份（保存到 `~/nightly_backups/`）
- ✅ 创建新目录结构
- ✅ 移动数据文件
- ✅ 重组脚本目录
- ✅ 归档历史数据
- ✅ 创建README文档
- ✅ 验证重组结果

### 步骤3: 更新路径引用

```bash
# 更新所有文件中的路径引用
python3 update_paths.py --execute

# 验证数据完整性
python3 tools/data_management/validate_raw_data.py
```

---

## 📊 重组前后对比

### 重组前 ❌

```
energy_dl/nightly/
├── scripts/                    # 40+个脚本混在一起
├── results/                    # 数据 + 22个历史目录 (2GB+)
│   ├── raw_data.csv           # 核心数据埋在深处
│   ├── run_20251201_*/        # 1.8GB历史数据
│   └── ...
```

访问核心文件：`results/raw_data.csv` (3层)

### 重组后 ✅

```
energy_dl/nightly/
├── data/                       # ⭐ 核心数据（清晰标识）
│   ├── raw_data.csv
│   └── data.csv
├── tools/                      # ⭐ 工具（按功能分类）
│   ├── data_management/       # 数据管理工具
│   ├── config_management/     # 配置管理工具
│   └── legacy/                # 历史脚本
├── archives/                   # ⭐ 历史数据（集中归档）
│   ├── runs/                  # 22个历史运行
│   └── data_snapshots/
```

访问核心文件：`data/raw_data.csv` (2层，减少1层)

---

## 🔍 验证清单

执行完成后，验证以下内容：

```bash
# ✅ 核心数据文件存在
ls -lh data/raw_data.csv
ls -lh data/data.csv

# ✅ 工具目录正确
ls tools/data_management/ | head -5
ls tools/config_management/ | head -5

# ✅ 历史数据已归档
ls archives/runs/ | wc -l  # 应显示22

# ✅ 数据完整性验证
python3 tools/data_management/validate_raw_data.py

# ✅ 测试核心功能
python3 tools/data_management/analyze_experiment_status.py
```

---

## 📁 新目录结构速查

| 目录 | 用途 | 说明 |
|------|------|------|
| `data/` | 核心数据 | raw_data.csv, data.csv ⭐ |
| `tools/data_management/` | 数据管理 | 验证、分析、修复脚本 ⭐ |
| `tools/config_management/` | 配置管理 | 配置生成、验证脚本 |
| `tools/legacy/` | 历史脚本 | 旧脚本归档（参考用） |
| `archives/runs/` | 历史运行 | 22个run_*目录 |
| `archives/data_snapshots/` | 历史快照 | summary_old.csv等 |
| `analysis/` | 因果分析 | 保持独立（不变） |
| `mutation/` | 训练核心 | 保持不变 |
| `docs/` | 项目文档 | 保持不变 |

---

## 🛠️ 常用命令更新

### 数据验证

```bash
# 之前
python3 scripts/validate_raw_data.py

# 之后
python3 tools/data_management/validate_raw_data.py
```

### 实验状态分析

```bash
# 之前
python3 scripts/analyze_experiment_status.py

# 之后
python3 tools/data_management/analyze_experiment_status.py
```

### 数据文件访问

```python
# 之前
df = pd.read_csv('results/raw_data.csv')

# 之后
df = pd.read_csv('data/raw_data.csv')
```

---

## ⚠️ 注意事项

### 1. 备份安全

- ✅ 脚本会自动创建完整备份
- ✅ 备份位置: `~/nightly_backups/nightly_backup_YYYYMMDD_HHMMSS.tar.gz`
- ✅ 如有问题可随时恢复

### 2. 路径更新

- ⚠️ 必须运行 `update_paths.py --execute` 更新路径引用
- ⚠️ 否则旧脚本会找不到文件

### 3. 分析模块

- ✅ `analysis/` 目录保持独立，不受影响
- ✅ 因果分析脚本路径不变

---

## 🔄 回滚方案

如果出现问题，可以快速回滚：

```bash
# 找到备份文件
ls -lth ~/nightly_backups/ | head -2

# 回滚操作
cd /home/green/energy_dl/nightly
rm -rf *
tar -xzf ~/nightly_backups/nightly_backup_YYYYMMDD_HHMMSS.tar.gz
```

---

## 📚 详细文档

- **完整方案**: `docs/RESTRUCTURE_PLAN_20260105.md`
- **数据目录**: `data/README.md`
- **工具目录**: `tools/README.md`
- **归档目录**: `archives/README.md`

---

## ✅ 完成后的下一步

1. **更新CLAUDE.md** - 更新项目结构快览
2. **更新文档路径** - 修正文档中的路径引用
3. **提交Git** - 提交重组变更（如使用Git）
4. **继续开发** - 使用新的目录结构进行开发

---

## 💡 预期效果

- ✅ **访问更快**: 核心文件层级减少
- ✅ **结构更清晰**: 功能模块明确分类
- ✅ **空间释放**: 历史数据集中归档
- ✅ **易于维护**: 活跃/历史文件清晰分离

---

**创建日期**: 2026-01-05
**维护者**: Claude Code
**状态**: 准备就绪 ✅
