"""
配置文件：精简版参数设置
"""

# ============================================================================
# 数据集配置（仅使用Adult数据集）
# ============================================================================
DATASET = 'adult'
SENSITIVE_ATTR = 'sex'  # 'sex' or 'race'
TEST_SIZE = 0.3  # 训练集:测试集 = 7:3

# ============================================================================
# 模型配置
# ============================================================================
MODEL_WIDTH = 4  # 神经网络宽度参数
HIDDEN_LAYERS = [64, 32, 16, 8, 4]  # 5层网络
EPOCHS = 20  # 减少训练轮数（原论文约50轮）
BATCH_SIZE = 128
LEARNING_RATE = 0.001

# ============================================================================
# 公平性方法配置（精简为4个）
# ============================================================================
FAIRNESS_METHODS = [
    'Reweighing',          # 预处理
    'AdversarialDebiasing', # 处理中
    'EqualizedOdds',       # 后处理
    'Baseline'             # 不应用任何方法
]

# 每个方法的α采样点（精简为5个）
ALPHA_VALUES = [0.0, 0.25, 0.5, 0.75, 1.0]

# ============================================================================
# 指标配置（精简为15个核心指标）
# ============================================================================
METRICS = {
    # 性能指标 (2个)
    'performance': ['Acc', 'F1'],

    # 群体公平性 (3个)
    'group_fairness': ['DI', 'SPD', 'AOD'],

    # 个体公平性 (2个，移除CDS以简化)
    'individual_fairness': ['Cons', 'TI'],

    # 鲁棒性 (2个，仅保留对抗攻击)
    'robustness': ['FGSM', 'PGD']
}

# 指标层次：数据集(D)、训练集(Tr)、测试集(Te)
METRIC_PHASES = ['D', 'Tr', 'Te']

# ============================================================================
# 因果图学习配置
# ============================================================================
DIBS_ITERATIONS = 2000  # 减少迭代次数（原论文10000）
DIBS_N_SAMPLES = 50     # 采样图的数量（原论文100）
EDGE_THRESHOLD = 0.5    # 边的概率阈值

# ============================================================================
# 权衡分析配置
# ============================================================================
# 需要分析的指标对
TRADEOFF_PAIRS = [
    # RQ1: Fairness vs Performance
    ('Te_SPD', 'Te_Acc'),
    ('Te_AOD', 'Te_Acc'),
    ('Te_TI', 'Te_F1'),

    # RQ3: Fairness vs Robustness (简化)
    ('Te_SPD', 'A_FGSM'),
    ('Te_AOD', 'A_PGD'),
]

# ============================================================================
# 路径配置
# ============================================================================
DATA_DIR = 'data'
RESULTS_DIR = 'results'
CAUSAL_GRAPH_PATH = f'{RESULTS_DIR}/causal_graph.pkl'
TRAINING_DATA_PATH = f'{DATA_DIR}/training_data.csv'
ANALYSIS_RESULTS_PATH = f'{RESULTS_DIR}/tradeoff_analysis.csv'

# ============================================================================
# 计算资源配置
# ============================================================================
DEVICE = 'cpu'  # 使用CPU，不需要GPU
N_JOBS = 1      # 单线程运行
RANDOM_SEED = 42
