# data.csv vs raw_data.csv 文件对比 ⭐⭐⭐

**创建日期**: 2025-12-22
**重大更新**: 2025-12-28 - 纠正关键理解错误
**目的**: 准确说明主项目两个CSV文件的区别，避免数据理解错误

---

## ⚠️ 关键理解 - 必读

### data.csv的核心设计

**data.csv不是简单的列删减，而是智能统一处理！**

```python
# data.csv的核心逻辑（来自create_unified_data_csv.py）
def get_field_value(row, field, is_parallel):
    """
    统一获取字段值，自动处理并行/非并行模式
    """
    if is_parallel:
        # 并行模式：优先使用fg_字段
        fg_value = row.get(f'fg_{field}', '').strip()
        if fg_value:
            return fg_value
    # 非并行模式或fallback：使用顶层字段
    return row.get(field, '').strip()
```

**含义**：
- ✅ data.csv中的`training_success`列已包含并行模式的`fg_training_success`数据
- ✅ data.csv中的`energy_gpu_avg_watts`已包含并行模式的`fg_energy_gpu_avg_watts`数据
- ✅ data.csv中的所有超参数、性能、能耗列都已统一处理
- ✅ **读取data.csv时，直接使用顶层字段名即可，无需考虑fg_前缀**

---

## 📊 文件基本信息

| 文件 | 路径 | 行数 | 列数 | 大小 |
|------|------|------|------|------|
| **raw_data.csv** | `results/raw_data.csv` | 726 | **87** | ~302KB |
| **data.csv** | `results/data.csv` | 726 | **56** | ~276KB |

**共同点**:
- 行数相同（726行，含header）
- 训练成功样本：726个（100%）⭐
- 有能耗数据：692个（95.3%）
- 有性能数据：~550个（取决于任务）

---

## 🔍 列差异详解

### raw_data.csv（87列）- 原始数据

**结构特点**：
- **非并行模式**：使用顶层字段（repository, model, training_success, energy_*, perf_*, hyperparam_*）
- **并行模式**：同时有顶层字段和fg_/bg_前缀字段

**并行模式的原始数据结构**：
```csv
experiment_id,repository,model,training_success,energy_gpu_avg_watts,fg_repository,fg_model,fg_training_success,fg_energy_gpu_avg_watts,...

exp_012_parallel,,,(空),,(空),pytorch_resnet,resnet20,True,224.6,...
```

**问题**：
- ❌ 并行模式的顶层字段为空
- ❌ 需要判断是否并行才能知道读哪个字段
- ❌ 数据分析代码复杂（需要if/else判断）

**列分类**：
```
87列 =
  9 基础信息（experiment_id, timestamp, repository, model, is_parallel, training_success, duration_seconds, retries, error_message）
+ 9 超参数（hyperparam_*）
+ 16 性能指标（perf_*）
+ 11 能耗指标（energy_*）
+ 4 实验元数据（experiment_source, num_mutated_params, mutated_param, mode）
+ 7 并行配置（bg_repository, bg_model, bg_note, bg_log_directory, fg_duration_seconds, fg_retries, fg_error_message）
+ 9 前台超参数（fg_hyperparam_*）
+ 16 前台性能指标（fg_perf_*）
+ 11 前台能耗指标（fg_energy_*）
+ 3 前台基础信息（fg_repository, fg_model, fg_training_success）
+ 其他列
```

---

### data.csv（56列）- 统一处理后

**核心改进**：✅ **所有字段已统一，无需判断并行模式！**

**统一处理的字段**：
- `training_success` - 自动合并了fg_training_success
- `duration_seconds` - 自动合并了fg_duration_seconds
- `repository` - 自动合并了fg_repository
- `model` - 自动合并了fg_model
- `energy_*` - 所有11个能耗字段都已合并fg_energy_*
- `perf_*` - 所有16个性能字段都已合并fg_perf_*
- `hyperparam_*` - 所有9个超参数字段都已合并fg_hyperparam_*

**统一后的数据结构**：
```csv
experiment_id,repository,model,is_parallel,training_success,energy_gpu_avg_watts,...

exp_001,examples,mnist,False,True,180.5,...              # 非并行：直接使用顶层字段
exp_012_parallel,pytorch_resnet,resnet20,True,True,224.6,...  # 并行：顶层=fg_字段
```

**列分类（56列）**：
```
56列 =
  9 基础信息（experiment_id, timestamp, repository, model, is_parallel, training_success, duration_seconds, retries, error_message）
+ 9 超参数（hyperparam_*，已统一）
+ 16 性能指标（perf_*，已统一）
+ 11 能耗指标（energy_*，已统一）
+ 4 实验元数据（experiment_source, num_mutated_params, mutated_param, mode）
+ 7 并行配置（bg_repository, bg_model, bg_note, bg_log_directory, fg_duration_seconds, fg_retries, fg_error_message）
```

**关键列说明**：

| 列名 | 非并行模式的值 | 并行模式的值 | 说明 |
|------|-------------|------------|------|
| `is_parallel` | False | True | 明确标记模式 |
| `repository` | 直接值（如examples） | fg_repository的值（如pytorch_resnet） | 已统一 |
| `training_success` | 顶层值（True/False） | fg_training_success的值 | 已统一 ⭐ |
| `energy_gpu_avg_watts` | 顶层值 | fg_energy_gpu_avg_watts的值 | 已统一 ⭐ |
| `hyperparam_learning_rate` | 顶层值 | fg_hyperparam_learning_rate的值 | 已统一 ⭐ |
| `perf_test_accuracy` | 顶层值 | fg_perf_test_accuracy的值 | 已统一 ⭐ |

---

## 🎯 使用建议

### 场景1: 能耗分析（本项目主要场景）⭐⭐⭐

**推荐文件**: **data.csv** ✅

**原因**:
1. ✅ 所有字段已统一，代码简洁
2. ✅ 不需要判断`is_parallel`再决定读哪个字段
3. ✅ 直接使用字段名（`training_success`, `energy_gpu_avg_watts`等）
4. ✅ 文件更小，加载更快

**正确的代码**：
```python
import pandas as pd

# ✅ 正确：直接读取data.csv
df = pd.read_csv('results/data.csv')

# ✅ 正确：直接使用字段名，无需考虑fg_前缀
successful = df[df['training_success'] == 'True']  # 包含所有726个训练成功的样本
has_energy = df[df['energy_gpu_avg_watts'].notna()]  # 692个有能耗的样本

# ✅ 正确：分析超参数对能耗的影响（726个样本）
df[['hyperparam_learning_rate', 'energy_gpu_avg_watts', 'perf_test_accuracy']]

# ✅ 正确：如需区分并行/非并行，使用is_parallel列
df_nonparallel = df[df['is_parallel'] == 'False']  # 621个非并行
df_parallel = df[df['is_parallel'] == 'True']      # 105个并行
```

**错误的代码**（不要这样做）：
```python
# ❌ 错误：在data.csv中检查fg_字段
if row['fg_training_success']:  # data.csv中fg_字段已合并到顶层！
    ...

# ❌ 错误：在data.csv中区分并行模式的字段
if is_parallel:
    energy = row['fg_energy_gpu_avg_watts']  # data.csv中不需要这样！
else:
    energy = row['energy_gpu_avg_watts']

# ✅ 正确：直接使用
energy = row['energy_gpu_avg_watts']  # 已自动包含并行模式数据
```

---

### 场景2: 并行模式深入分析

**推荐文件**: **raw_data.csv** ✅

**适用情况**:
- 需要对比前台任务（fg_）和后台任务（bg_）的数据
- 需要分析并行模式的资源分配
- 需要研究前台-后台任务的相互影响

**示例**:
```python
import pandas as pd

# 读取raw_data.csv
df = pd.read_csv('results/raw_data.csv')

# 筛选并行实验（通过mode列）
df_parallel = df[df['mode'] == 'parallel']  # 105个

# 对比前台和后台任务
comparison = df_parallel[[
    'fg_energy_gpu_avg_watts',  # 前台能耗
    'bg_energy_gpu_avg_watts',  # 后台能耗
    'fg_repository',            # 前台任务
    'bg_repository'             # 后台任务
]]
```

**什么时候需要raw_data.csv**:
- ❌ 一般的能耗分析 → 用data.csv
- ❌ 超参数影响分析 → 用data.csv
- ❌ 性能-能耗权衡 → 用data.csv
- ✅ 研究"前台vs后台"资源竞争 → 用raw_data.csv
- ✅ 分析并行干扰效应 → 用raw_data.csv

---

## 📋 关键差异总结

| 维度 | raw_data.csv | data.csv |
|------|--------------|----------|
| **列数** | 87 | 56 |
| **并行模式处理** | 保留fg_/bg_原始结构 | 统一合并到顶层字段 ⭐ |
| **代码复杂度** | 高（需判断并行模式） | 低（直接使用字段名） ⭐ |
| **适用场景** | 并行模式深入分析 | 能耗分析、因果分析 ⭐⭐⭐ |
| **推荐度** | ⭐⭐ | ⭐⭐⭐⭐⭐ |

---

## ✅ 训练成功判断（重要更正）⭐⭐⭐

### 错误理解 ❌

```python
# ❌ 错误：以为需要同时检查顶层和fg_字段
def is_training_success(row):
    if row.get('training_success') == 'True':
        return True
    if row.get('fg_training_success') == 'True':
        return True
    return False
```

### 正确理解 ✅

```python
# ✅ 正确：data.csv中直接检查顶层字段即可
def is_training_success(row):
    return row.get('training_success') == 'True'

# data.csv中training_success已包含：
#   - 非并行模式：顶层training_success的值
#   - 并行模式：fg_training_success的值（自动合并）
```

### 实际数据验证

```
data.csv统计：
  training_success == 'True': 726个（100%）
  包括：
    - 非并行模式：621个
    - 并行模式：105个（来自fg_training_success）
```

**结论**：
- ✅ data.csv中，所有726个样本的training_success都是'True'
- ✅ 不存在"训练失败"的情况（100%成功率）
- ✅ 之前认为有105个"训练失败"是**理解错误**（实际是并行模式的成功样本）

---

## 📊 数据完整性

| 指标 | 数量 | 百分比 | 说明 |
|------|------|--------|------|
| **总样本** | 726 | 100% | - |
| **训练成功** | 726 | 100% | 所有样本训练成功 ⭐ |
| **有能耗数据** | 692 | 95.3% | 34个因监控故障无能耗 |
| **有性能数据** | ~550 | ~75.8% | 取决于任务类型 |
| **非并行模式** | 621 | 85.5% | - |
| **并行模式** | 105 | 14.5% | - |

**能耗缺失的34个样本**：
- ⚠️ 不是训练失败
- ⚠️ 是能耗监控系统故障（2025-12-14/16）
- ⚠️ 主要影响VulBERTa任务

---

## 🎯 本项目的选择

### 当前使用：data.csv ✅

**复制到analysis模块**：
```bash
# 已完成
cp results/data.csv analysis/data/energy_research/raw/energy_data_original.csv
```

**选择理由**：
1. ✅ 字段已统一，无需复杂判断
2. ✅ 包含所有726个训练成功样本
3. ✅ 有能耗数据的692个样本完全够用
4. ✅ 代码简洁，易于维护
5. ✅ 适合因果分析（DiBS, DML, 中介效应等）

### 不使用raw_data.csv的原因

**对于能耗分析**：
- ❌ 不需要区分fg_/bg_字段
- ❌ 不研究"前台vs后台"资源竞争
- ❌ data.csv的统一字段已满足需求

**如果未来需要**：
- 研究并行模式的详细机制
- 分析前台-后台任务的干扰效应

**则可切换到**：raw_data.csv

---

## 🚀 关键要点回顾

### 1. data.csv不是简单删减列

**错误理解**：data.csv = raw_data.csv删除一些列
**正确理解**：data.csv = raw_data.csv经过**智能统一处理**

### 2. 并行模式的fg_字段已合并

**错误理解**：data.csv中没有并行模式的fg_数据
**正确理解**：data.csv的顶层字段**已包含**并行模式的fg_数据

### 3. training_success列已统一

**错误理解**：需要同时检查training_success和fg_training_success
**正确理解**：data.csv中training_success**已自动包含**并行模式数据

### 4. 训练成功率100%

**错误理解**：有105个训练失败样本
**正确理解**：726个样本全部训练成功，105个是并行模式成功样本

### 5. 直接使用字段名

**错误代码**：
```python
if is_parallel:
    energy = row['fg_energy_gpu_avg_watts']
else:
    energy = row['energy_gpu_avg_watts']
```

**正确代码**：
```python
energy = row['energy_gpu_avg_watts']  # 已自动处理并行模式
```

### 6. 唯一标识 = experiment_id + timestamp ⭐⭐⭐ **[2025-12-28 新增]**

**重要提醒**：experiment_id **不是**唯一标识符！

**设计特性**：
- ✅ 不同轮次的实验可以使用相同的experiment_id
- ✅ 同一配置可以在不同时间运行多次
- ✅ 726行数据中有637个唯一experiment_id（89条重复记录）

**错误代码**：
```python
# ❌ 错误：用experiment_id作为字典键会丢失数据
data_dict = {}
for row in rows:
    data_dict[row['experiment_id']] = row  # 会丢失89条记录！
```

**正确代码**：
```python
# ✅ 正确：使用复合键
df['unique_key'] = df['experiment_id'] + '|' + df['timestamp']

# 或者直接去重
df_dedup = df.drop_duplicates(subset=['experiment_id', 'timestamp'])

# 或者选择最佳记录
df_best = select_best_records(df)  # 见唯一标识说明文档
```

**详细说明**：参见 [DATA_UNIQUENESS_CLARIFICATION_20251228.md](DATA_UNIQUENESS_CLARIFICATION_20251228.md) ⭐⭐⭐

---

## 📝 文档更新日志

- **2025-12-22**: 创建初版
- **2025-12-28**: 重大更新
  - ✅ 纠正列数错误（54 → 56）
  - ✅ 纠正统一处理机制的理解
  - ✅ 纠正训练成功判断逻辑
  - ✅ 明确data.csv的fg_字段合并机制
  - ✅ 更新数据完整性统计（726行，100%训练成功）
  - ✅ **添加唯一标识说明（experiment_id + timestamp）** ⭐⭐⭐

---

**文档维护者**: Analysis模块
**最后更新**: 2025-12-28
**重要性**: ⭐⭐⭐⭐⭐ 必读文档，避免数据理解错误
