# energy_data_original.csv 列使用分析

**创建日期**: 2025-12-22
**数据源**: `data/energy_research/raw/energy_data_original.csv` (54列)
**分析方案**: 方案A（协变量方法）

---

## 📊 列使用总览

| 类别 | 总列数 | 已纳入 | 未纳入 | 纳入率 |
|------|--------|--------|--------|--------|
| **超参数** | 9 | 6 | 3 | 66.7% |
| **模型标识** | 2 | 2 | 0 | 100% |
| **训练模式** | 1 | 1 | 0 | 100% |
| **能耗指标** | 11 | 3 | 8 | 27.3% |
| **性能指标** | 14 | 2 | 12 | 14.3% |
| **时间指标** | 1 | 1 | 0 | 100% |
| **元数据** | 16 | 0 | 16 | 0% |
| **总计** | 54 | 15 | 39 | 27.8% |

---

## 🔍 详细列清单

### 1. 超参数类（9列）

#### ✅ 已纳入（6列）- 作为输入变量（因）

| 列名 | 原意 | 因果角色 | 为何纳入 |
|------|------|----------|----------|
| `hyperparam_learning_rate` | 学习率 | 干预方法 | **主要超参数**，直接影响训练速度和能耗 |
| `hyperparam_batch_size` | 批量大小 | 干预方法 | **主要超参数**，影响GPU内存使用和能耗 |
| `hyperparam_epochs` | 训练轮数 | 干预方法 | **主要超参数**，决定训练时长和总能耗 |
| `hyperparam_dropout` | Dropout比率 | 干预方法 | 正则化参数，影响模型复杂度和训练时间 |
| `hyperparam_weight_decay` | 权重衰减 | 干预方法 | 正则化参数，影响优化过程 |
| `hyperparam_seed` | 随机种子 | 干预方法 | **用户要求添加**，在主项目中被变异的超参数 ⭐ |

#### ❌ 未纳入（3列）

| 列名 | 原意 | 为何未纳入 | 建议 |
|------|------|------------|------|
| `hyperparam_alpha` | Alpha参数 | **模型特定参数**（仅部分模型使用，如SVM的C参数），缺失率可能很高 | 可选：如果某些模型大量使用，可作为可选输入 |
| `hyperparam_kfold` | K折交叉验证折数 | **实验设置参数**，不是训练超参数，不直接影响单次训练的能耗和性能 | 不建议纳入：属于评估策略而非训练参数 |
| `hyperparam_max_iter` | 最大迭代次数 | **模型特定参数**（仅用于迭代优化器如SGD的某些实现），大部分深度学习用epochs | 可选：如果数据中广泛使用且与epochs不重复，可考虑纳入 |

---

### 2. 模型标识类（2列）

#### ✅ 已纳入（2列）- 作为协变量

| 列名 | 原意 | 因果角色 | 为何纳入 |
|------|------|----------|----------|
| `repository` | 代码仓库名 | 协变量 | 与`model`组合为`model_id`，One-Hot编码为11列 |
| `model` | 模型名称 | 协变量 | 与`repository`组合为`model_id`，One-Hot编码为11列 |

**处理方式**:
```python
df['model_id'] = df['repository'] + '/' + df['model']
# 生成11个模型: examples/mnist, VulBERTa/mlp, Person_reID/densenet121, ...
df_encoded = pd.get_dummies(df, columns=['model_id'], prefix='model')
```

---

### 3. 训练模式类（1列）

#### ✅ 已纳入（1列）- 作为敏感属性

| 列名 | 原意 | 因果角色 | 为何纳入 |
|------|------|----------|----------|
| `is_parallel` | 是否并行训练（Boolean） | 敏感属性 | **用户指定**，类比Adult中的性别，研究并行模式的调节作用 ⭐ |

---

### 4. 能耗指标类（11列）

#### ✅ 已纳入（3列）- 作为输出变量（果）

| 列名 | 原意 | 因果角色 | 为何纳入 |
|------|------|----------|----------|
| `energy_gpu_avg_watts` | GPU平均功率（瓦特） | 主要结果 | **核心能耗指标**，反映GPU瞬时功耗 ⭐ |
| `energy_gpu_total_joules` | GPU总能耗（焦耳） | 主要结果 | **核心能耗指标**，反映GPU累计能耗 ⭐ |
| `energy_cpu_total_joules` | CPU总能耗（焦耳） | 主要结果 | **核心能耗指标**，反映CPU累计能耗 ⭐ |

#### ❌ 未纳入（8列）

| 列名 | 原意 | 为何未纳入 | 建议 |
|------|------|------------|------|
| `energy_cpu_pkg_joules` | CPU包能耗（焦耳） | **细粒度指标**，已被`energy_cpu_total_joules`包含（total = pkg + ram） | 可选：如需分析CPU包vs内存能耗差异 |
| `energy_cpu_ram_joules` | CPU内存能耗（焦耳） | **细粒度指标**，已被`energy_cpu_total_joules`包含 | 可选：如需分析内存能耗占比 |
| `energy_gpu_max_watts` | GPU最大功率（瓦特） | **统计指标**，与avg高度相关，信息冗余 | 不推荐：增加变量数但信息增益小 |
| `energy_gpu_min_watts` | GPU最小功率（瓦特） | **统计指标**，与avg高度相关，信息冗余 | 不推荐：增加变量数但信息增益小 |
| `energy_gpu_temp_avg_celsius` | GPU平均温度（摄氏度） | **辅助指标**，温度是能耗的结果而非独立结果变量 | 可选：如研究"超参数 → 温度"因果关系 |
| `energy_gpu_temp_max_celsius` | GPU最大温度（摄氏度） | **辅助指标**，与avg相关 | 不推荐：信息冗余 |
| `energy_gpu_util_avg_percent` | GPU平均利用率（百分比） | **辅助指标**，利用率是中介变量而非最终结果 | 可选：如研究"超参数 → 利用率 → 能耗"路径 |
| `energy_gpu_util_max_percent` | GPU最大利用率（百分比） | **辅助指标**，与avg相关 | 不推荐：信息冗余 |

---

### 5. 性能指标类（14列）

#### ✅ 已纳入（2列）- 作为输出变量（果）

| 列名 | 原意 | 因果角色 | 为何纳入 |
|------|------|----------|----------|
| `perf_test_accuracy` | 测试准确率 | 主要结果 | **核心性能指标**，反映模型泛化能力 ⭐ |
| `perf_test_loss` | 测试损失 | 主要结果 | **核心性能指标**，与accuracy互补 ⭐ |

#### ❌ 未纳入（12列）

| 列名 | 原意 | 为何未纳入 | 建议 |
|------|------|------------|------|
| `perf_best_val_accuracy` | 最佳验证准确率 | **训练过程指标**，与test_accuracy高度相关 | 可选：如研究"验证 vs 测试"差异（过拟合检测） |
| `perf_final_training_loss` | 最终训练损失 | **训练过程指标**，与test_loss相关但可能有过拟合 | 可选：如研究过拟合的因果关系 |
| `perf_map` | 平均精度均值（Mean Average Precision） | **任务特定指标**，仅用于检索任务（如Person_reID） | 可选：如只分析检索模型 |
| `perf_precision` | 精确率 | **任务特定指标**，仅部分任务使用 | 可选：如研究分类详细指标 |
| `perf_recall` | 召回率 | **任务特定指标**，仅部分任务使用 | 可选：如研究分类详细指标 |
| `perf_rank1` | Rank-1准确率 | **任务特定指标**，仅用于检索任务 | 不推荐：适用范围窄 |
| `perf_rank5` | Rank-5准确率 | **任务特定指标**，仅用于检索任务 | 不推荐：适用范围窄 |
| `perf_top1_accuracy` | Top-1准确率 | **与test_accuracy重复**，同一指标的不同名称 | 不推荐：信息冗余 |
| `perf_top5_accuracy` | Top-5准确率 | **扩展指标**，与top1相关但信息增益小 | 可选：如研究Top-K指标差异 |
| `perf_top10_accuracy` | Top-10准确率 | **扩展指标**，与top1相关 | 不推荐：信息增益小 |
| `perf_top20_accuracy` | Top-20准确率 | **扩展指标**，与top1相关 | 不推荐：信息增益小 |
| `perf_eval_samples_per_second` | 评估速度（样本/秒） | **吞吐量指标**，更适合作为性能（而非准确率）的补充 | 可选：如研究"超参数 → 推理速度"因果关系 |

---

### 6. 时间指标类（1列）

#### ✅ 已纳入（1列）- 作为输出变量（果）

| 列名 | 原意 | 因果角色 | 为何纳入 |
|------|------|----------|----------|
| `duration_seconds` | 训练时长（秒） | 中介变量/结果 | **重要指标**，连接"超参数 → 时长 → 能耗"因果路径 ⭐ |

---

### 7. 元数据类（16列）

#### ❌ 全部未纳入（16列）

| 列名 | 原意 | 为何未纳入 |
|------|------|------------|
| `experiment_id` | 实验标识符（非唯一） | **标识符**，无因果意义，仅用于数据管理。⚠️ **注意**：不同轮次的实验可使用相同ID，需与timestamp组合才能唯一标识一条记录 |
| `timestamp` | 实验时间戳 | **标识符**，与experiment_id组合形成唯一标识（experiment_id + timestamp），无因果意义 |
| `training_success` | 训练是否成功（Boolean） | **过滤条件**，仅保留成功实验，不作为变量 |
| `retries` | 训练重试次数 | **日志信息**，反映实验稳定性但无因果意义 |
| `error_message` | 错误信息（字符串） | **日志信息**，仅失败实验有值 |
| `experiment_source` | 实验来源（default/mutation） | **元信息**，描述实验类型但无因果意义 |
| `num_mutated_params` | 变异参数数量 | **元信息**，描述实验设计但无因果意义 |
| `mutated_param` | 被变异的参数名 | **元信息**，描述实验设计但无因果意义 |
| `mode` | 模式（default/mutation/parallel等） | **已被is_parallel替代**，is_parallel是mode的简化版本 |
| `bg_repository` | 背景任务代码仓库 | **并行实验元信息**，在方案A中被is_parallel抽象 |
| `bg_model` | 背景任务模型 | **并行实验元信息**，在方案A中被is_parallel抽象 |
| `bg_note` | 背景任务备注 | **日志信息**，无因果意义 |
| `bg_log_directory` | 背景任务日志目录 | **日志信息**，无因果意义 |
| `fg_duration_seconds` | 前景任务时长 | **并行实验细节**，在方案A中已有duration_seconds |
| `fg_retries` | 前景任务重试次数 | **并行实验细节**，无因果意义 |
| `fg_error_message` | 前景任务错误信息 | **并行实验细节**，无因果意义 |

**注**: 元数据列主要用于数据管理、实验追踪、日志记录，不具有因果意义，因此不纳入因果分析。

---

## 📋 方案A修正后的变量设计

### 输入变量（18个）- Causes

```python
inputs = [
    # 1. 超参数（连续型）- 6个 [干预方法] ⭐ 添加seed
    'learning_rate',      # 学习率
    'batch_size',         # 批量大小
    'epochs',             # 训练轮数
    'dropout',            # Dropout比率
    'weight_decay',       # 权重衰减
    'seed',               # 随机种子 ⭐ 新增

    # 2. 并行模式（二值）- 1个 [敏感属性]
    'is_parallel',        # 0=非并行, 1=并行

    # 3. 模型类型（One-Hot）- 11个 [协变量]
    'model_examples/mnist',
    'model_examples/mnist_ff',
    'model_examples/mnist_rnn',
    'model_examples/siamese',
    'model_pytorch_resnet_cifar10/resnet20',
    'model_MRT-OAST/default',
    'model_VulBERTa/mlp',
    'model_bug-localization-by-dnn-and-rvsm/default',
    'model_Person_reID_baseline_pytorch/densenet121',
    'model_Person_reID_baseline_pytorch/hrnet18',
    'model_Person_reID_baseline_pytorch/pcb'
]
```

### 输出变量（6个）- Effects

```python
outputs = [
    # 1. 能耗指标 - 3个 [主要关注结果]
    'energy_gpu_avg_watts',       # GPU平均功率（主要关注）⭐
    'energy_gpu_total_joules',    # GPU总能耗
    'energy_cpu_total_joules',    # CPU总能耗

    # 2. 性能指标 - 2个 [主要关注结果]
    'perf_test_accuracy',         # 测试准确率（主要关注）⭐
    'perf_test_loss',             # 测试损失

    # 3. 训练时长 - 1个 [中介变量]
    'duration_seconds'            # 训练时长
]
```

**总变量数**: 18 + 6 = **24个变量**（原23个，添加seed后+1）

---

## 🔄 变量纳入原则总结

### 纳入标准 ✅

1. **输入变量（因）**:
   - 超参数：主项目中被变异的超参数（learning_rate, batch_size, epochs, dropout, weight_decay, seed）
   - 敏感属性：用户指定的分组变量（is_parallel）
   - 协变量：控制混淆的变量（model类型）

2. **输出变量（果）**:
   - 核心能耗指标：avg（瞬时功耗）+ total（累计能耗）
   - 核心性能指标：accuracy + loss
   - 中介变量：duration（连接超参数和能耗）

### 排除标准 ❌

1. **信息冗余**：max/min与avg高度相关
2. **任务特定**：仅部分模型使用的指标（map, rank1等）
3. **元数据**：无因果意义的标识符和日志信息
4. **细粒度过高**：已被更高层指标包含（cpu_pkg/ram vs cpu_total）
5. **实验设置**：属于实验设计而非训练参数（kfold）

---

## 💡 可选扩展建议

### 阶段1完成后（方案A验证成功），可考虑添加：

#### 1. 辅助能耗指标（如关注温度和利用率）
```python
# 扩展能耗分析
outputs_extended = outputs + [
    'energy_gpu_temp_avg_celsius',    # 研究温度因果关系
    'energy_gpu_util_avg_percent'     # 研究利用率因果关系
]
```

#### 2. 训练过程指标（如关注过拟合）
```python
# 扩展性能分析
outputs_extended = outputs + [
    'perf_best_val_accuracy',         # 研究验证-测试差异
    'perf_final_training_loss'        # 研究训练-测试差异
]
```

#### 3. 模型特定超参数（如某些模型广泛使用）
```python
# 检查alpha和max_iter的使用情况
alpha_usage = df['hyperparam_alpha'].notna().sum()
max_iter_usage = df['hyperparam_max_iter'].notna().sum()

# 如果使用率 > 50%，可考虑添加到inputs
if alpha_usage > len(df) * 0.5:
    inputs_extended = inputs + ['alpha']
```

---

## 📊 变量使用统计

### 输入变量覆盖率

| 变量 | 数据中的列名 | 预期缺失率 | 处理方式 |
|------|-------------|-----------|----------|
| learning_rate | hyperparam_learning_rate | 低（<5%） | 默认值0.001填充 |
| batch_size | hyperparam_batch_size | 低（<5%） | 默认值32填充 |
| epochs | hyperparam_epochs | 低（<5%） | 默认值10填充 |
| dropout | hyperparam_dropout | 中（~30%） | 默认值0.0填充（无dropout） |
| weight_decay | hyperparam_weight_decay | 中（~30%） | 默认值0.0填充（无正则） |
| seed | hyperparam_seed | 低（<10%） | 默认值42填充 |
| is_parallel | is_parallel | 无缺失 | Boolean，无需处理 |
| model | repository + model | 无缺失 | 组合后One-Hot编码 |

### 输出变量覆盖率

| 变量 | 数据中的列名 | 预期可用率 | 处理方式 |
|------|-------------|-----------|----------|
| energy_gpu_avg_watts | energy_gpu_avg_watts | 91.1% | 删除缺失样本 |
| energy_gpu_total_joules | energy_gpu_total_joules | 91.1% | 删除缺失样本 |
| energy_cpu_total_joules | energy_cpu_total_joules | 91.1% | 删除缺失样本 |
| perf_test_accuracy | perf_test_accuracy | 91.1% | 删除缺失样本 |
| perf_test_loss | perf_test_loss | 91.1% | 删除缺失样本 |
| duration_seconds | duration_seconds | 100% | 无缺失 |

**有效样本数**: 676 × 91.1% = **616个**（与主项目数据一致）

---

**文档状态**: ✅ 完成 - 列使用分析
**创建时间**: 2025-12-22
**维护者**: Analysis模块
