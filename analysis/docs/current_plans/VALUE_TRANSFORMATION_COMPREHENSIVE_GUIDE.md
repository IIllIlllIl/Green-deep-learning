# 值转换方案综合指南

**创建日���**: 2026-01-23
**文档来源**: 合并了3个相关文档
- RELATIVE_VALUE_TRANSFORMATION_PLAN.md（方案设计）
- RELATIVE_VALUE_TRANSFORMATION_SUMMARY.md（总结与验证）
- DIFFERENCE_VALUE_TRANSFORMATION_PLAN_V2.md（差值转换v2）

**状态**: ✅ 验证完成，准备实施

---

## 📋 执行摘要

### 问题

并行/非并行训练���式存在系统性能耗偏差，导致DiBS因果分析时可能错误识别`is_parallel`为强因果因子，掩盖超参数的真实因果效应。

### 解决方案

**值转换**: 将所有能耗字段转换为相对于默认值实验的相对值或差值，消除系统性偏差。

**两种转换方式**:
1. **相对值转换**（主要推荐）: `相对值 = (实际值 - 基准值) / 基准值`
2. **差值转换**（备���方案）: `差值 = 实际值 - 基准值`

其中基准值使用**稳健统计量**（中位数 + 离群值剔除）计算。

### 关键优势

1. ✅ **消除系统性偏差**: is_parallel的效应将显著减弱
2. ✅ **突出超参数效应**: 相对变化更能反映超参数的真实影响
3. ✅ **保留因果结构**: 不改变变量间的依赖关系
4. ✅ **数据完整性**: 所有6组都有完整的默认值实验数据

---

## 🎯 两种转换方式对比

| 维度 | 相对值转换 | 差值转换 |
|------|-----------|---------|
| **公式** | (实际值-基准值)/基准值 | 实际值-基准值 |
| **结果含义** | 百分比变化（如+0.5表示增加50%） | 绝对变化（如+500J） |
| **可解释性** | ⭐⭐⭐ 直观，跨组可比 | ⭐⭐ 需要结合基准值理解 |
| **DiBS兼容性** | ⭐⭐⭐ 推荐 | ⭐⭐ 可用 |
| **推荐场景** | 因果分析、跨组对比 | 简单描述性分析 |

**推荐**: 优先使用**相对值转换**，因为结果更直观且易于解释。

---

## ✅ 已完成的工作

### 1. 默认值实验识别（2026-01-16）

**成果**: 成功识别所有6组的默认值实验

| 组别 | 模型 | 默认值实验数 | 并行 | 非并行 | 数据来源 |
|------|------|-------------|------|--------|---------|
| Group1 | examples | 10 | 4 | 6 | 6groups数据 |
| Group2 | VulBERTa/mlp | 2 | 1 | 1 | data.csv恢复 ⭐ |
| Group3 | Person_reID | 6 | 3 | 3 | 6groups数据 |
| Group4 | bug-localization | 2 | 1 | 1 | data.csv恢复 ⭐ |
| Group5 | MRT-OAST | 2 | 1 | 1 | 6groups数据 |
| Group6 | ResNet | 2 | 1 | 1 | 6groups数据 |
| **总计** | - | **24** | **11** | **13** | - |

**关键说明**:
- Group2和Group4的默认值实验从`data.csv`中恢复
- 这些实验在6groups数据中缺失的原因：缺少性能指标
- 能耗数据完整且与raw_data.csv一致（已验证）

**输出文件**: `data/energy_research/identified_default_experiments.json`

---

### 2. 数据来源验证（2026-01-16）

**验证目标**: 确认data.csv和raw_data.csv中Group2/4默认值实验的能耗数据一致性

**验证结果**: ✅ **完全一致**

#### Group2 (VulBERTa/mlp)
- ✅ 非并行实验: CPU能耗89,815.23J, GPU能耗726,127.40J
- ✅ 并行实验: CPU能耗143,366.48J, GPU能耗847,272.32J

#### Group4 (bug-localization)
- ✅ 非并行实验: CPU能耗61,899.84J, GPU能耗22,795.91J
- ✅ 并行实验: CPU能耗85,803.35J, GPU能耗227,915.45J

**结论**: ✅ data.csv和raw_data.csv的能耗数据完全一致，可以安全使用data.csv中恢复的默认值数据。

---

### 3. 稳健基准值计算（2026-01-16）

**计算方法**:
1. 收集每组的所有默认值实验
2. 按并行/非并行模式分组
3. 对每个能耗字段：
   - 剔除离群值（z-score > 2.5）
   - 计算中位数（主要基准值）
   - 计算平均值和标准差（参考）
   - 计算变异系数（CV，评估稳定性）

**输出文件**: `data/energy_research/baseline_values_robust.json`

---

## 📊 核心发现

### 基准值稳定性分析

| 组别 | 默认值实验数 | CPU能耗CV | GPU能耗CV | 稳定性评估 |
|------|-------------|----------|-----------|-----------|
| **Group1** (examples) | 10 (4并行, 6非并行) | 86-108% | 90-165% | ⚠️ 变异较大 |
| **Group2** (VulBERTa) | 2 (1并行, 1非并行) | N/A | N/A | ⚠️ 样本不足 |
| **Group3** (Person_reID) | 6 (3并行, 3非并行) | 15-18% | 17% | ✅ 非常稳定 |
| **Group4** (bug-loc) | 2 (1并行, 1非并行) | N/A | N/A | ⚠️ 样本不足 |
| **Group5** (MRT-OAST) | 2 (1并行, 1非并行) | N/A | N/A | ⚠️ 样本不足 |
| **Group6** (ResNet) | 2 (1并行, 1非并行) | N/A | N/A | ⚠️ 样本不足 |

**关键观察**:
1. ✅ **Group3最稳定**: Person_reID组有6个默认值实验，CV < 20%，最适合相对值转换
2. ⚠️ **Group1变异较大**: examples组包含4个不同模型，默认值变异较大（CV > 80%）
3. ⚠️ **Group2/4/5/6样本不足**: 只有1个并行+1个非并行实验，无法计算标准差

**应对策略**:
- **Group3**: 使用中位数基准值，预期效果最好
- **Group1**: 使用中位数（比平均值更稳健），但预期相对值仍有较大变异
- **Group2/4/5/6**: 使用唯一值作为基准值（相当于平均值/中位数）

---

## 🚀 下一步实施计划

### 阶段1: 生成转换数据

**任务**: 实现转换脚本，生成6组转换数据

**输入**:
- 6groups原始数据: `data/energy_research/6groups_*.csv`
- 稳健基准值: `data/energy_research/baseline_values_robust.json`

**输出**:
- 6组转换数据: `data/energy_research/6groups_*_transformed.csv`
- 基准值记录: `data/energy_research/baseline_values_applied.json`

**转换规则**:

**相对值转换**:
```python
# 能耗字段转换为相对值（7个字段）
relative_value = (actual_value - baseline) / baseline
```

**差值转换**:
```python
# 能耗字段转换为差值（7个字段）
difference_value = actual_value - baseline
```

**需要转换的能耗字段**（7个）:
- energy_cpu_pkg_joules
- energy_cpu_ram_joules
- energy_cpu_total_joules
- energy_gpu_total_joules
- energy_gpu_avg_watts
- energy_gpu_max_watts
- energy_gpu_min_watts

**保持不变的字段**:
- 温度/利用率（4个字段）: energy_gpu_temp_avg_celsius, energy_gpu_temp_max_celsius, energy_gpu_util_avg_percent, energy_gpu_util_max_percent
- 控制变量和性能指标

**脚本路径**: `scripts/generate_value_transformed_data.py`

---

### 阶段2: 验证转换结果

**验证步骤**:

1. **验证1: 默认值实验转换值应为0（相对值）或接近0（差值）**
   - 检查所有默认值实验的转换值
   - 相对值容忍度: ±0.01
   - 差值容忍度: ±1.0

2. **验证2: is_parallel效应显著减弱**
   - 比较转换前后is_parallel对能耗的影响
   - 预期: 系统性偏差显著降低

3. **验证3: 转换值分布合理性**
   - 相对值分布范围（预期: -1 ~ +5）
   - 差值分布范围（预期: -基准值 ~ +数倍基准值）

4. **验证4: 数据完整性**
   - 记录数不变
   - 非能耗字段不变
   - 无意外缺失值

---

### 阶段3: DiBS因果分析

**使用转换数据进行DiBS分析**:
- 输入: 6组转换数据
- 预期: is_parallel效应减弱，超参数因果效应更清晰

---

## 📝 生成脚本需求

### 脚本功能需求

**脚本名称**: `scripts/generate_value_transformed_data.py`

**核心功能**:
1. 读取6groups原始数据
2. 读取稳健基准值（baseline_values_robust.json）
3. 对每条记录：
   - 根据is_parallel选择对应基准值（并行/非并行）
   - 对7个能耗字段应用值转换
   - 保留其他字段不变
4. 生成6组转换CSV文件
5. 记录应用的基准值信息

**命令行参数**:
```bash
python3 scripts/generate_value_transformed_data.py \
  --input-dir data/energy_research \
  --baseline baseline_values_robust.json \
  --method relative  # relative 或 difference
  --output-suffix _transformed \
  --dry-run  # 可选：测试模式
```

**输出文件** (6+1):
- `data/energy_research/6groups_group1_examples_transformed.csv`
- `data/energy_research/6groups_group2_vulberta_transformed.csv`
- `data/energy_research/6groups_group3_person_reid_transformed.csv`
- `data/energy_research/6groups_group4_buglocalization_transformed.csv`
- `data/energy_research/6groups_group5_mrt_oast_transformed.csv`
- `data/energy_research/6groups_group6_resnet_transformed.csv`
- `data/energy_research/baseline_values_applied.json`

---

## 📚 相关文档

| 文档 | 说明 |
|------|------|
| [archived/obsolete_plans/value_transformation_originals/](../../archived/obsolete_plans/value_transformation_originals/) | 原始未合并文档（3个） |
| [QUESTION1_REGRESSION_ANALYSIS_PLAN.md](../current_plans/QUESTION1_REGRESSION_ANALYSIS_PLAN.md) | 回归分析计划（上下文） |
| [DATA_FILES_COMPARISON.md](../essential_guides/DATA_FILES_COMPARISON.md) | data.csv vs raw_data.csv对比 |

---

## 📌 版本历史

| 版本 | 日期 | 修改内容 |
|------|------|---------|
| v1.0 | 2026-01-23 | 合并3个值转换文档，创建综合指南 |

---

**维护者**: Claude
**最后更新**: 2026-01-23
**状态**: ✅ 验证完成，准备实施
