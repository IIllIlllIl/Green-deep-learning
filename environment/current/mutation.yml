name: mutation_runner
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    # mutation.py本身不需要特殊依赖
    # 只使用Python标准库: json, subprocess, argparse, pathlib, re, time, datetime
    # 但为了完整性，列出建议的工具包

    # 可选：用于结果分析
    # - pandas
    # - matplotlib
    # - numpy

    # 可选：用于JSON验证
    # - jsonschema
