# Enhanced CSV 数据可靠性分析报告

**生成日期**: 2025-12-11
**分析对象**: results/summary_all_enhanced.csv
**实验总数**: 476个
**原始列数**: 37列
**增强后列数**: 46列 (+9列)

---

## 📊 执行摘要

本报告分析了enhanced CSV文件的当前状态，评估了可以安全添加的派生变量，并提供了基于数据可靠性的建议。

### 关键发现

✅ **数据完整性优秀**：所有能耗相关的核心数据100%完整
✅ **可安全添加10个派生变量**：基于现有数据，风险低
⚠️ **数据集规模信息缺失**：需要外部数据源，不建议添加
✅ **已有9个增强列**：包括training_mode, model_type, dataset等

---

## 1️⃣ 当前状态检查

### 你要求的变量 - 状态总结

| 变量 | 状态 | 列名 | 数据完整性 |
|------|------|------|-----------|
| ✅ 最大GPU能耗 | 已存在 | `energy_gpu_max_watts` | 100% (476/476) |
| ✅ 最小GPU能耗 | 已存在 | `energy_gpu_min_watts` | 100% (476/476) |
| ✅ GPU利用率 | 已存在 | `energy_gpu_util_avg_percent` | 100% (476/476) |
| ✅ 数据集名称 | 已存在 | `dataset` | 100% (476/476) |
| ❌ CPU能耗占比 | **未添加** | - | - |
| ❌ RAM能耗占比 | **未添加** | - | - |
| ❌ GPU能耗占比 | **未添加** | - | - |
| ❌ GPU功率差值 | **未添加** | - | - |
| ❌ 数据集规模 | **未添加** | - | - |

### 已添加的9个增强列

1. `training_mode` - 训练模式（顺序/并行）
2. `model_type` - 模型类型（CNN/RNN/Transformer等）
3. `model_size` - 模型规模（small/medium/large）
4. `dataset` - 数据集名称
5. `task` - 任务类型（分类/检测等）
6. `energy_total_joules` - 总能耗（CPU+GPU）
7. `energy_per_second` - 单位时间能耗
8. `energy_per_epoch` - 单个epoch能耗
9. `gpu_efficiency` - GPU效率评分

---

## 2️⃣ 数据完整性分析

### 能耗数据完整性 ✅

所有能耗相关的核心列都达到**100%数据完整性**：

```
energy_cpu_total_joules         : 476/476 (100.0%) ✅
energy_gpu_total_joules         : 476/476 (100.0%) ✅
energy_gpu_max_watts            : 476/476 (100.0%) ✅
energy_gpu_min_watts            : 476/476 (100.0%) ✅
energy_gpu_util_avg_percent     : 476/476 (100.0%) ✅
energy_cpu_ram_joules           : 476/476 (100.0%) ✅
energy_gpu_avg_watts            : 476/476 (100.0%) ✅
energy_gpu_temp_max_celsius     : 476/476 (100.0%) ✅
energy_gpu_temp_avg_celsius     : 476/476 (100.0%) ✅
```

**结论**：能耗监控系统运行稳定，数据质量优秀，可以作为派生变量计算的可靠基础。

---

## 3️⃣ 可安全添加的变量（✅ 推荐）

### 高优先级（强烈推荐）⭐⭐⭐

#### 1. `energy_cpu_percent` - CPU能耗占比

**公式**：
```python
energy_cpu_percent = (energy_cpu_total_joules /
                      (energy_cpu_total_joules + energy_gpu_total_joules)) * 100
```

**可靠性**：⭐⭐⭐⭐⭐ 高
**风险等级**：低
**数据来源**：100%完整的原始能耗数据
**价值**：直接回答"CPU占总能耗的多少百分比"

#### 2. `energy_gpu_percent` - GPU能耗占比

**公式**：
```python
energy_gpu_percent = (energy_gpu_total_joules /
                      (energy_cpu_total_joules + energy_gpu_total_joules)) * 100
```

**可靠性**：⭐⭐⭐⭐⭐ 高
**风险等级**：低
**数据来源**：100%完整的原始能耗数据
**价值**：直接回答"GPU占总能耗的多少百分比"

#### 3. `energy_gpu_power_range` - GPU功率波动范围

**公式**：
```python
energy_gpu_power_range = energy_gpu_max_watts - energy_gpu_min_watts
```

**可靠性**：⭐⭐⭐⭐⭐ 高
**风险等级**：低
**数据来源**：nvidia-smi实时监控数据
**价值**：反映GPU负载稳定性，值越大说明波动越大

**示例数据**：
- MRT-OAST: 318.31 - 66.75 = **251.56瓦** （波动大）
- mnist_ff: 98.68 - 76.53 = **22.15瓦** （波动小）

#### 4. `cpu_gpu_energy_ratio` - CPU/GPU能耗比

**公式**：
```python
cpu_gpu_energy_ratio = energy_cpu_total_joules / energy_gpu_total_joules
```

**可靠性**：⭐⭐⭐⭐⭐ 高
**风险等级**：低
**数据来源**：100%完整的原始能耗数据
**价值**：一个数字直观比较CPU和GPU的能耗关系

**解释**：
- 比值 > 1：CPU能耗高于GPU（CPU密集型任务）
- 比值 < 1：GPU能耗高于CPU（GPU密集型任务）
- 比值 ≈ 0.1：GPU能耗是CPU的10倍（典型深度学习训练）

---

### 中优先级（推荐）⭐⭐

#### 5. `energy_ram_percent` - RAM能耗占比（在CPU能耗中）

**公式**：
```python
energy_ram_percent = (energy_cpu_ram_joules / energy_cpu_total_joules) * 100
```

**可靠性**：⭐⭐⭐⭐⭐ 高
**风险等级**：低
**数据来源**：perf工具监控的RAM能耗
**价值**：了解内存访问在CPU能耗中的占比

**注意**：这个比例是相对于CPU总能耗，不是全局能耗。

#### 6. `training_speed` - 训练速度

**公式**：
```python
training_speed = duration_seconds / hyperparam_epochs  # 秒/epoch
```

**可靠性**：⭐⭐⭐⭐ 高
**风险等级**：低
**数据可用性**：29.4% (140/476) - 仅限有epochs参数的实验
**价值**：标准化比较不同实验的训练效率

**限制**：
- 仅适用于基于epoch的训练（140个实验）
- bug-localization等基于迭代次数的实验无法使用

#### 7. `gpu_power_efficiency` - GPU功率效率

**公式**：
```python
gpu_power_efficiency = energy_gpu_util_avg_percent / energy_gpu_avg_watts
```

**可靠性**：⭐⭐⭐⭐⭐ 高
**风险等级**：低
**数据来源**：100%完整的GPU监控数据
**价值**：评估GPU在不同功率下的利用效率

**解释**：值越高说明单位功率下的利用率越高（更高效）

#### 8. `temperature_range` - GPU温度波动范围

**公式**：
```python
temperature_range = energy_gpu_temp_max_celsius - energy_gpu_temp_avg_celsius
```

**可靠性**：⭐⭐⭐⭐⭐ 高
**风险等级**：低
**数据来源**：nvidia-smi温度监控
**价值**：反映散热压力，值越大说明峰值负载期间温度上升越多

---

### 低优先级（可选）⭐

#### 9. `energy_gpu_power_std_ratio` - GPU功率变异系数

**公式**：
```python
energy_gpu_power_std_ratio = (energy_gpu_max_watts - energy_gpu_min_watts) /
                              energy_gpu_avg_watts
```

**可靠性**：⭐⭐⭐⭐ 高
**风险等级**：低
**价值**：标准化的功率波动指标，便于跨实验比较

#### 10. `energy_efficiency_score` - 能效评分

**公式**：
```python
energy_efficiency_score = (perf_test_accuracy / energy_total_joules) * 1000000
```

**可靠性**：⭐⭐⭐ 中
**风险等级**：中
**数据可用性**：34.0% (162/476)
**限制**：依赖于perf_test_accuracy的完整性

**注意**：仅对有测试精度数据的实验有意义。

---

## 4️⃣ 不推荐添加的变量（❌ 风险高）

### 1. ❌ `dataset_size` - 数据集样本数量

**为何不安全**：
- 数据来源不在实验记录中
- 需要手动查阅每个数据集的文档
- 不同模型可能使用不同的子集
- 容易出现不一致或错误

**风险等级**：中
**替代方案**：如果需要，建议创建单独的数据集元数据文件，而非混入实验数据

### 2. ❌ `model_parameters` - 模型参数量

**为何不安全**：
- 需要深入分析每个模型的架构代码
- 不同超参数配置可能导致参数量变化（如dropout层）
- 维护成本高，容易过时

**风险等级**：高
**替代方案**：建议使用`model_size`（small/medium/large）的定性分类

### 3. ❌ `dataset_dimensions` - 输入数据维度

**为何不安全**：
- 需要查阅数据集文档
- 可能因数据预处理而变化
- 不同模型对同一数据集的预处理可能不同

**风险等级**：中

---

## 5️⃣ 推荐的实施方案

### 方案A：保守方案（4个高优先级变量）

**推荐给**：对数据质量要求极高的场景

**添加变量**：
1. `energy_cpu_percent` - CPU能耗占比
2. `energy_gpu_percent` - GPU能耗占比
3. `energy_gpu_power_range` - GPU功率波动范围
4. `cpu_gpu_energy_ratio` - CPU/GPU能耗比

**优点**：
- ✅ 100%数据完整性
- ✅ 计算简单，不易出错
- ✅ 直接回答用户关心的问题

**实施难度**：⭐ 低

---

### 方案B：标准方案（8个变量）⭐ **推荐**

**推荐给**：标准分析需求

**添加变量**：
- 方案A的4个变量
- `energy_ram_percent` - RAM能耗占比
- `training_speed` - 训练速度（有epochs的实验）
- `gpu_power_efficiency` - GPU功率效率
- `temperature_range` - GPU温度波动

**优点**：
- ✅ 覆盖你要求的所有关键指标
- ✅ 数据完整性高
- ✅ 分析价值大

**实施难度**：⭐⭐ 中低

---

### 方案C：完整方案（10个变量）

**推荐给**：需要深度分析的研究场景

**添加变量**：
- 方案B的8个变量
- `energy_gpu_power_std_ratio` - GPU功率变异系数
- `energy_efficiency_score` - 能效评分（部分实验）

**注意**：
- ⚠️ `energy_efficiency_score`仅对34%的实验有效
- ⚠️ 需要在分析时注意缺失值处理

**实施难度**：⭐⭐⭐ 中等

---

## 6️⃣ 数据质量保证措施

### 计算前验证

在添加任何派生列之前，脚本应验证：

1. **数据完整性检查**：
   ```python
   assert all(row['energy_cpu_total_joules'] for row in rows), "CPU能耗数据有缺失"
   assert all(row['energy_gpu_total_joules'] for row in rows), "GPU能耗数据有缺失"
   ```

2. **数值有效性检查**：
   ```python
   # 能耗值应为正数
   assert float(row['energy_cpu_total_joules']) > 0, "能耗值应大于0"

   # 百分比应在0-100之间
   assert 0 <= cpu_percent <= 100, "百分比应在0-100之间"
   ```

3. **逻辑一致性检查**：
   ```python
   # CPU% + GPU% 应约等于 100%
   total_percent = cpu_percent + gpu_percent
   assert 99.9 <= total_percent <= 100.1, "能耗占比之和应为100%"
   ```

### 计算后验证

1. **统计摘要**：生成每个新列的min/max/mean/std
2. **异常值检测**：标记超出3σ的异常值
3. **相关性检查**：验证派生变量之间的逻辑关系

---

## 7️⃣ 建议的列顺序

为了提高可读性，建议按以下顺序组织列：

### 分组1：基本信息
- experiment_id, timestamp, repository, model, experiment_source

### 分组2：训练配置
- training_mode, model_type, model_size, dataset, task

### 分组3：超参数
- hyperparam_* (所有超参数)

### 分组4：性能指标
- perf_* (所有性能指标)

### 分组5：能耗原始数据
- energy_cpu_*, energy_gpu_* (原始能耗数据)

### 分组6：能耗派生指标（⭐ 新增）
- energy_cpu_percent
- energy_gpu_percent
- energy_ram_percent
- cpu_gpu_energy_ratio
- energy_gpu_power_range
- gpu_power_efficiency
- temperature_range

### 分组7：其他派生指标
- training_speed, energy_efficiency_score

### 分组8：元数据
- training_success, duration_seconds, retries

---

## 8️⃣ 示例数据预览

### 高优先级变量计算示例

以实验 `default__MRT-OAST_default_001` 为例：

**原始数据**：
- energy_cpu_total_joules: 39,987.66
- energy_gpu_total_joules: 331,876.33
- energy_gpu_max_watts: 318.31
- energy_gpu_min_watts: 66.75
- energy_gpu_avg_watts: 270.26

**计算结果**：
```python
# 1. CPU能耗占比
energy_cpu_percent = 39987.66 / (39987.66 + 331876.33) * 100 = 10.75%

# 2. GPU能耗占比
energy_gpu_percent = 331876.33 / (39987.66 + 331876.33) * 100 = 89.25%

# 3. GPU功率波动范围
energy_gpu_power_range = 318.31 - 66.75 = 251.56瓦

# 4. CPU/GPU能耗比
cpu_gpu_energy_ratio = 39987.66 / 331876.33 = 0.120
```

**解释**：
- GPU消耗了89.25%的总能耗（典型的GPU密集型深度学习任务）
- CPU仅消耗10.75%（主要用于数据加载和预处理）
- GPU功率波动很大（251.56瓦），说明负载变化明显
- CPU能耗仅为GPU的12%

---

## 9️⃣ 实施建议

### 立即行动（今天）

1. **采用方案B**（8个变量）- 标准方案
2. 创建Python脚本添加这8个列
3. 运行数据质量验证
4. 生成统计摘要报告

### 脚本实施清单

```python
# 必需步骤
1. 备份原文件: cp summary_all_enhanced.csv summary_all_enhanced.csv.backup
2. 读取CSV并验证数据完整性
3. 计算8个派生变量
4. 添加数据验证逻辑
5. 写入新CSV文件
6. 生成验证报告
7. 更新文档
```

### 验证清单

- [ ] 所有476行都成功计算了新列
- [ ] 没有NaN或空值（除了training_speed可能有缺失）
- [ ] CPU% + GPU% ≈ 100%（允许±0.1%误差）
- [ ] GPU功率范围 ≥ 0
- [ ] 生成了统计摘要（min/max/mean/std）
- [ ] 更新了README.md和文档

---

## 🎯 总结

### 可以安全添加的变量（方案B推荐）

✅ **100%可靠，强烈推荐添加**：
1. energy_cpu_percent - CPU能耗占比
2. energy_gpu_percent - GPU能耗占比
3. energy_gpu_power_range - GPU功率波动范围
4. cpu_gpu_energy_ratio - CPU/GPU能耗比
5. energy_ram_percent - RAM能耗占比
6. gpu_power_efficiency - GPU功率效率
7. temperature_range - GPU温度波动

⚠️ **部分数据，谨慎添加**：
8. training_speed - 训练速度（29.4%实验有效）

❌ **不推荐添加**：
- dataset_size - 数据集规模（需要外部数据）
- model_parameters - 模型参数量（维护成本高）
- dataset_dimensions - 数据维度（数据源不可靠）

### 为什么这些变量是安全的？

1. **数据完整性**：所有基础能耗数据100%完整
2. **计算简单**：都是基础算术运算，不易出错
3. **逻辑可验证**：可以通过逻辑关系验证结果正确性
4. **无需外部数据**：全部基于实验记录的原始数据

---

**报告生成者**: Claude Code
**项目版本**: v4.7.2
**报告状态**: ✅ 已完成
**下一步**: 实施方案B - 添加8个安全可靠的派生变量
